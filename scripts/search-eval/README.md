# 관광지 검색 품질 평가 — 판정 세트와 3구성 nDCG@10

`hybrid-search-local-embedding`(ADR-0090) 의 품질 근거다. **이 폴더가 판정 세트의 원본이다** — 2026-09-21 까지는
OCI 노드 `/tmp` 에만 있었고, 재부팅 한 번이면 「하이브리드가 BM25 보다 낫다」를 다시 증명할 길이 없었다.

| 파일 | 무엇 |
|---|---|
| `judgments-attractions-2026-09-13.json` | 쿼리 48개(ko 24 · en 24) × 관광지 id 등급 0~3, 2,141건. 풀링은 세 구성의 top-10 합집합 |
| `live-eval.py` | A. BM25 · B. 하이브리드(RRF) · C. 하이브리드 + 쿼리 언더스탠딩(라이브 API) 의 nDCG@10 |
| `run-eval.sh` | 노드에서 포트포워드 두 개(OpenSearch · 인코더 사이드카)를 열고 위를 돌린다 |
| `results/<날짜>.json` | 남길 값이 있던 실행의 결과 |

## 돌리는 법

```bash
scp -r scripts/search-eval msa-oci:/tmp/ && ssh msa-oci bash /tmp/search-eval/run-eval.sh
```

## 기준선 (2026-09-13)

| 구성 | ko | en |
|---|---|---|
| A. BM25 단독 | 0.4584 | 0.5712 |
| B. 하이브리드 | 0.6094 | 0.6646 |
| C. 하이브리드 + 쿼리 언더스탠딩 | **0.6448** | **0.6883** |

## 2026-09-22 — 메모리 한도 2560Mi 뒤 같은 세트로 재측정

| 구성 | ko | en |
|---|---|---|
| A. BM25 단독 | 0.4305 | 0.5465 |
| B. 하이브리드 | 0.5954 | 0.6420 |
| C. 하이브리드 + 쿼리 언더스탠딩 | **0.6584** | **0.6602** |

09-13 과의 차이는 색인 드리프트다 — 그 사이 관광지가 더 들어와(개요 백필) 판정 없는 문서가 top-10 에 섞인다.
순서(A < B < C)와 폭은 그대로다. 메모리 한도는 속도만 바꿨고 순위는 안 바꿨다는 뜻이다.

## 함정

- **판정 세트는 그것을 만든 시스템에만 공정하다.** 새 구성(예: 1-bit 양자화)이 top-10 에 새 문서를 올리면 그 문서는
  등급이 없어 0 으로 계산된다. 큰 변경 뒤엔 풀링을 다시 하고 등급을 더한 뒤 비교한다.
- 매일 도는 `search-eval-daily` CronJob 은 이 세트를 쓰지 않는다 — ADR-0050 의 상품 검색 기준선 잡이고
  `analytics.search_judgments` 가 비어 매일 건너뛴다(2026-09-21 확인). 관광지 평가는 이 폴더의 스크립트다.
- C(라이브)−B(색인 직접) 델타를 전부 쿼리 언더스탠딩 탓으로 읽지 않는다 — 앱 질의 모양·인덱스 드리프트가 섞인다.
