apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# host-access — 로컬 호스트에서 인프라 직접 접근용 LoadBalancer Services.
# LOCAL 전용. k3s-lite overlay 에서만 사용. prod-k8s 에서는 NEVER include.
resources:
  - services.yaml
  - network-policy.yaml
