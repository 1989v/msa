apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: opensearch
  namespace: commerce
  labels:
    app.kubernetes.io/name: opensearch
    app.kubernetes.io/part-of: commerce-platform
spec:
  serviceName: opensearch-headless
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: opensearch
  template:
    metadata:
      labels:
        app.kubernetes.io/name: opensearch
    spec:
      terminationGracePeriodSeconds: 30
      initContainers:
        # Single-node OpenSearch 도 ES 와 동일하게 vm.max_map_count 하한이 필요.
        - name: increase-vm-max-map
          image: busybox:1.36
          command: ["sh", "-c", "sysctl -w vm.max_map_count=262144 || true"]
          securityContext:
            privileged: true
            runAsUser: 0
      containers:
        - name: opensearch
          # ADR-0055 — ES 9.0.0 에서 전환. opensearch-java 3.8.0 클라이언트와 정렬.
          image: opensearchproject/opensearch:3.3.0
          imagePullPolicy: IfNotPresent
          # Korean nori analyzer 플러그인 보장 — search(products) + code-dictionary 공용
          command:
            - bash
            - -c
            - |
              if ! /usr/share/opensearch/bin/opensearch-plugin list | grep -q analysis-nori; then
                /usr/share/opensearch/bin/opensearch-plugin install --batch analysis-nori
              fi
              exec /usr/share/opensearch/opensearch-docker-entrypoint.sh opensearch
          ports:
            - name: http
              containerPort: 9200
            - name: transport
              containerPort: 9300
          env:
            - name: discovery.type
              value: "single-node"
            # 로컬 단일노드 — security plugin off (ES 의 xpack.security.enabled=false 등가)
            - name: DISABLE_SECURITY_PLUGIN
              value: "true"
            - name: DISABLE_INSTALL_DEMO_CONFIG
              value: "true"
            - name: OPENSEARCH_JAVA_OPTS
              value: "-Xms512m -Xmx512m"
            - name: cluster.name
              value: "commerce-local"
          readinessProbe:
            httpGet:
              path: /_cluster/health?local=true
              port: 9200
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 9200
            initialDelaySeconds: 90
            periodSeconds: 30
          resources:
            requests:
              cpu: 200m
              memory: 1Gi
            limits:
              memory: 1536Mi
          volumeMounts:
            - name: data
              mountPath: /usr/share/opensearch/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 3Gi
