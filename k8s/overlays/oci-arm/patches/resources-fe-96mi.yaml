# Tier FE — nginx 정적 자산 서빙. 실측 RSS ~30Mi.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: placeholder
spec:
  template:
    spec:
      containers:
        - name: app
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi   # FE 4종 실사용 4~5Mi — nginx 정적 서빙
