apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: commerce
  labels:
    app.kubernetes.io/name: auth
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: auth
  ports:
    - name: http
      port: 8087
      targetPort: http
