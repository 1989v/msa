apiVersion: v1
kind: Service
metadata:
  name: product
  namespace: commerce
  labels:
    app.kubernetes.io/name: product
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: product
  ports:
    - name: http
      port: 8081
      targetPort: http
