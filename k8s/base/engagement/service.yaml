apiVersion: v1
kind: Service
metadata:
  name: engagement
  namespace: commerce
  labels:
    app.kubernetes.io/name: engagement
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: engagement
  ports:
    - name: http
      port: 8091
      targetPort: http
