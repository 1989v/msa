apiVersion: v1
kind: Service
metadata:
  name: account
  namespace: commerce
  labels:
    app.kubernetes.io/name: account
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: account
  ports:
    - name: http
      port: 8093
      targetPort: http
