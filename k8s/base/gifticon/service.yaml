apiVersion: v1
kind: Service
metadata:
  name: gifticon
  namespace: commerce
  labels:
    app.kubernetes.io/name: gifticon
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: gifticon
  ports:
    - name: http
      port: 8086
      targetPort: http
