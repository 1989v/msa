apiVersion: v1
kind: Service
metadata:
  name: place
  namespace: commerce
  labels:
    app.kubernetes.io/name: place
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: place
  ports:
    - name: http
      port: 8096
      targetPort: http
