apiVersion: v1
kind: Service
metadata:
  name: search
  namespace: commerce
  labels:
    app.kubernetes.io/name: search
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: search
  ports:
    - name: http
      port: 8083
      targetPort: http
