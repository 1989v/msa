apiVersion: v1
kind: Service
metadata:
  name: admin-fe
  namespace: commerce
  labels:
    app.kubernetes.io/name: admin-fe
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: admin-fe
  ports:
    - name: http
      port: 80
      targetPort: http
