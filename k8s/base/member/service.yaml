apiVersion: v1
kind: Service
metadata:
  name: member
  namespace: commerce
  labels:
    app.kubernetes.io/name: member
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: member
  ports:
    - name: http
      port: 8093
      targetPort: http
