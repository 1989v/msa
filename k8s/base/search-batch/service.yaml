apiVersion: v1
kind: Service
metadata:
  name: search-batch
  namespace: commerce
  labels:
    app.kubernetes.io/name: search-batch
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: search-batch
  ports:
    - name: http
      port: 8092
      targetPort: http
