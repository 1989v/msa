apiVersion: v1
kind: Service
metadata:
  name: content
  namespace: commerce
  labels:
    app.kubernetes.io/name: content
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: content
  ports:
    - name: http
      port: 8097
      targetPort: http
