apiVersion: v1
kind: Service
metadata:
  name: inventory
  namespace: commerce
  labels:
    app.kubernetes.io/name: inventory
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: inventory
  ports:
    - name: http
      port: 8085
      targetPort: http
