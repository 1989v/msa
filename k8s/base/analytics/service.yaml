apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: commerce
  labels:
    app.kubernetes.io/name: analytics
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: analytics
  ports:
    - name: http
      port: 8090
      targetPort: http
