apiVersion: v1
kind: Service
metadata:
  name: chatbot
  namespace: commerce
  labels:
    app.kubernetes.io/name: chatbot
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: chatbot
  ports:
    - name: http
      port: 8086
      targetPort: http
