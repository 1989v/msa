apiVersion: v1
kind: Service
metadata:
  name: order
  namespace: commerce
  labels:
    app.kubernetes.io/name: order
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: order
  ports:
    - name: http
      port: 8082
      targetPort: http
