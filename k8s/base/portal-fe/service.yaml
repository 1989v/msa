apiVersion: v1
kind: Service
metadata:
  name: portal-fe
  namespace: commerce
  labels:
    app.kubernetes.io/name: portal-fe
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: portal-fe
  ports:
    - name: http
      port: 80
      targetPort: http
