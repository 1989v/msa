apiVersion: v1
kind: Service
metadata:
  name: quant
  namespace: commerce
  labels:
    app.kubernetes.io/name: quant
    app.kubernetes.io/part-of: commerce-platform
spec:
  selector:
    app.kubernetes.io/name: quant
  ports:
    - name: http
      port: 8094
      targetPort: http
