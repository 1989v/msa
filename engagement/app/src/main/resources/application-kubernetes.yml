spring:
  datasource:
    url: jdbc:mysql://mysql-experiment-master:3306/experiment_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    ads:
      url: jdbc:mysql://mysql-ads-master:3306/ads_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8&useUnicode=true
  data:
    redis:
      host: redis-1
      port: 6379

recommendation:
  # 자기 자신을 부른다 — 같은 파드라 localhost 로 두어 Service 왕복을 줄인다.
  experiment:
    url: http://localhost:8091

analytics:
  service:
    url: http://analytics:8090
