# Key decisions

열린 pre-impl 질문: 0 건

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-09-20 | 운영자 판별은 **로그인(ROLE_ADMIN) 기준만** | 게스트 운영자는 서버가 구별할 수 없다. 브라우저 옵트아웃 스위치는 기기마다 켜야 하는 상태라 거절 — 테스트는 로그인 상태로 한다 |
| 2026-09-20 | 정리 대상은 자동화 15행 + `00` 2행. `go`·`가즈아`·`스넥크` 는 남긴다 | 사용자 선택 |
| 2026-09-20 | `CrawlerUserAgents` 를 `common` 으로 올려 analytics·game 이 공유 | 마커 목록은 운영 관찰로 자라는 지식 — 따로 들면 한쪽만 갱신된다. blog 의 목록은 뜻이 달라(curl·preview) 그대로 |
| 2026-09-20 | 화면 문구는 `lib/rank.js` 만. 아레나는 소스만 고치고 재게시하지 않는다 | 번들 해시가 바뀌면 배포 확인 항목이 늘어난다. **다음 게시까지 배포된 아레나는 제외 응답을 「연습 순위표 0위 · N점 (최고 기록 유지)」로 그린다** |
| 2026-09-20 | 제외 응답은 200 + `excluded=true` (BusinessException 아님) | 운영자 플레이마다 warn 로그가 남고, 위젯이 「잠시 후 다시 시도」를 띄운다. ADR-0095 원장의 202/`accepted=0` 와 같은 모양 |
| 2026-09-20 | 숨김 컬럼이 아니라 **쓰지 않는다** | 조회 지점 다섯 중 하나만 빠져도 새고, 스키마 변경이 든다 |
| 2026-09-20 (리뷰 1회차) | `auth.js` 를 안 싣는 게임 22종에 한 줄씩 넣는다 | 안 넣으면 그 22종에서는 로그인해도 운영자 판별이 없다. 22종 전부 code UI 가 없어 `.game-signed-in` 숨김 규칙에 걸리는 요소가 없다 |
| 2026-09-20 (리뷰 1회차) | 정리는 패턴으로 찾고 id 로 지운다 | 미리보기와 실행 사이에 끼어드는 행이 없게. `'00'` 은 실제 플레이어가 고를 수 있는 닉이라 패턴 삭제를 문서에 남기지 않는다 |
| 2026-09-20 (리뷰 1회차) | 필드명 `isOperator`/`isAutomation` | 같은 BC 의 `ReplyToGameSuggestionUseCase.Command.isOperator` 와 같은 개념·같은 이름 |
| 2026-09-20 (리뷰 1회차) | 컨트롤러 조립 테스트(`GameScoreControllerTest`) 추가 | 헤더 → Command 배선이 곧 게이트인데 V5 는 UA 에서 먼저 걸려 운영자 배선을 못 잰다 |
