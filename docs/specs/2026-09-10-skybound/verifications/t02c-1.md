# T02C-1 PC 시야 검증 · 2026-09-11

우클릭 드래그 회전, 휠 줌(3~8), pitch 제한(.08~1.15), 카메라 방향 기준 WASD를 기존 traversal fixture에 연결했다. reset은 yaw0/pitch.29/distance5.2로 돌아간다. pointer capture 해제·취소·blur·pause 시 드래그를 해제한다.

## 직접 실행한 검증

- `node --test implementation/t02b/traversal/tests/*.test.mjs`: **6 tests, pass 6, fail 0**.
- `node implementation/t02b/traversal/build.mjs`: **Built traversal/viewer.bundle.js**.
- `check-controls.mjs 9403 <traversal URL>`: **passed 11, failed 0, errors []**.
  실제 CDP 우클릭 드래그/휠, 회전 후 W 이동, blur, 합성 pointercancel, reset 확인.
- `check-traversal.mjs 9403 <traversal URL> t02c-1-regression`: **passed 19, failed 0, errors []**.
  실제 지형 접지·이동·점프/낙하/착지·키보드·blur 회귀 확인.
- 증거: [시야 검사 JSON](t02c-1-browser.json), [이동 회귀 JSON](t02c-1-regression-browser.json), [화면](t02c-1-world.png).

소프트웨어 WebGL에서 기능을 검증했다. FPS/실제 기기 성능 판정은 하지 않았다. 터치 입력, 카메라 장애물 충돌, 캡슐/사물 충돌 및 최종 아트는 미완료다. 다음은 T02C-2 터치 입력이다.
