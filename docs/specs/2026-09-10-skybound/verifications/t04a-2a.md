# T04A-2a · 프리즘 데모 연결

- Build: `node implementation/t02b/traversal/build.mjs` → Built traversal/viewer.bundle.js.
- Unit: `node --test implementation/t04a/tests/*.test.mjs implementation/t02b/traversal/tests/*.test.mjs` → tests 29, pass 29, fail 0.
- Browser: `node verifications/check-manipulation.mjs 9403 http://127.0.0.1:8768/implementation/t02b/traversal/index.html` → passed 11, failed 0.
- 증거: [JSON](t04a-2a-browser.json), [화면](t04a-2a.png).

실제 E/R 입력, committed 상태 분리, 운반 이동·점프 잠금, pause 동결, drop 반영, CDP 터치 집기, 겹침 배치 거부, 취소/전체 reset 복구, 브라우저 오류 없음 확인. 화면은 기본 조준/프리즘/벽/버튼 가독성만 검토했으며 최종 아트 승인이 아니다.

남음: 경사/가장자리의 물체 전체 지지, 벽 sweep 화면 경계, 취소 시 actor 겹침 및 작은 모바일 뷰포트 QA. 터치는 데스크톱 크기의 CDP 에뮬레이션이며 실기기 검증이 아니다. 퍼즐 완료/세이브 미연결. 브라우저 종료로 재실행 시 ECONNREFUSED가 발생하여 같은 명령에서 격리 프로필을 재기동했다.
