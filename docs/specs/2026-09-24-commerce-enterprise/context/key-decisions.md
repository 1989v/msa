# Key Decisions

열린 pre-impl 질문: 0 건

- 2026-09-24 COMPLETED 는 구매 확정으로 재정의 (사용자)
- 2026-09-24 주문서는 order 읽기 모델로 계산, 할인은 견적 · 최종 판정은 TCC reserve (사용자)
- 2026-09-24 정산식에서 환불 상계 제외 — 환불은 구매 확정 전 라인만이라 이중 차감 (리뷰 domain N3·implementation N1)
- 2026-09-24 부분 취소는 주문 상태가 아니라 라인 상태 + refunded_amount (리뷰 usecase N2)
- 2026-09-24 운영 실측: orders 0 · outbox_event 0(order·inventory·fulfillment) · inventory 0 · products 24 — 주문이 한 번도 끝까지 간 적 없다. 전환 마이그레이션은 빈 데이터 대상
- 2026-09-24 아웃박스: attempts 는 NOT NULL DEFAULT 0(옛 코드와 공존 무해), 10번째 실패에 FAILED, 릴레이 트랜잭션 READ COMMITTED(REPEATABLE READ 에서 갭 잠금 교착 재현)
- 2026-09-24 기존 결함 발견: common OutboxEntity 에 no-arg 생성자가 없어 옛 릴레이가 행을 못 읽었다(행이 0이라 드러나지 않음)
- 2026-09-24 사용자 쿠폰 상태는 별도 토픽 없이 `promotion.hold.*` 의 userCouponStatus 로 order 읽기 모델에 전달(토픽 추가 안 함)
- 2026-09-24 promotion reserve 는 주문서 견적 couponDiscount 를 싣고, 재계산과 다르면 failed(DISCOUNT_MISMATCH) — 결제액 불일치 방지. 쿠폰 유효기간 [from, until), 판매자 쿠폰 최소금액은 그 판매자 라인 기준, 배송비는 쿠폰 대상 제외
- 2026-09-24 안분 잔차가 최대 라인 금액을 넘으면 그 라인은 자기 금액까지만 받고 나머지는 다음 큰 라인으로(라인 결제액 음수 방지)
- 2026-09-24 포인트 상한은 쿠폰 뒤 상품 금액(배송비 제외) — 포인트는 라인에 안분되고 배송비 라인은 받지 않는다. 초과는 422
- 2026-09-24 남의 주문서 GET 은 404(존재 누설 방지), 주문 생성 시 소유자·재사용·만료는 422
- 2026-09-24 ACTIVE 예약 전환은 phase 0 SmartLifecycle — 만료 스케줄러·리스너보다 먼저 돌아야 결제된 옛 예약이 만료되지 않는다

## 미결 24건 처리 결정 (2026-09-25, 사용자 지시 「미결건 다 진행」)
- Q1·Q13 토스 운영 활성화: **하지 않는다**(commerce 상시 파드 egress 금지 ADR-0031/0070 유지). 대신 어댑터 계약을 바로잡는다 — 토스는 confirm 이 곧 매입이므로 AUTHORIZED 없이 CAPTURED 로 보고, 웹훅은 서명 헤더 대신 「웹훅은 재조회 신호일 뿐」으로(본문·헤더 불신, 레이트 리밋). → W3
- Q2 메모리: 운영 실측 947MiB / 1200Mi(79%) — Q6(풀 크기 무적용) 수정으로 줄이고 재측정. → W4
- Q3 전역 예외 처리기: basePackages 로 범위를 좁힌다. → W1
- Q4 admin FE 가 부르는 없는 API: 상품 DELETE = 어드민 판매 중지(INACTIVE, 삭제 아님)로 구현, 주문 상태 PATCH 는 사가가 소유하므로 FE 호출을 지운다. → W1
- Q5 quant `/api/v1/orders` 경로 충돌: quant 경로를 `/api/v1/quant/...` 로 옮긴다. → W4
- Q6 hikari 풀 무적용: 설정이 실제로 바인딩되게 고치고 런타임 값을 테스트로 읽는다. → W4
- Q7 게이트웨이 ROLE_ADMIN 상위 통과: **유지**(전역 규약). 판매자 포털은 서비스가 판매자 행으로 판정한다 — 문서화만.
- Q8 반려 판매자 파기: 상호까지 파기(개인사업자 상호는 개인정보일 수 있다 — 보수적으로). 이력에는 id·상태·사유만. → W4
- Q9 auth 늦은 이벤트: 회원별 마지막 반영 시각 테이블로 옛 이벤트를 버린다. → W4
- Q10 어드민 상품 등록: 항상 플랫폼 판매자 1. → W1
- Q11 배지 대비: TG12 에서 이미 수정(라이트 8.24:1) — 닫음.
- Q12 판매자 상품 목록: 판매자 전용 전체 상태 목록 + pageable 정렬 적용. → W1
- Q14 대사 날짜 경계: 결제 행과 모의 PG 원장이 같은 매입 시각을 쓰게 한다(한 값을 양쪽에 기록). → W3
- Q15 판매자 표시: 상호를 seller 이벤트와 order 읽기 모델에 싣고 FE 에 표시. 판매 불가 품목은 전체 422 유지(부분 생성은 사용자 혼동). 「포털 홈」 링크 대비 수정. → W2
- Q16 @EnableKafka 위치: CommerceApplication 으로 옮긴다(app 에 spring-kafka 의존 추가 — 호스트 전체 설정이 한 도메인 설정에 있으면 그 도메인을 빼는 순간 전부 꺼진다). 테스트 auto-startup 은 팩토리에 적용. → W1
- Q17 이행 라인 식별: orderItemId 로 식별(같은 상품 두 라인 허용), release 뒤 늦은 reserve 는 release 답이 있으면 거절, 쓰이지 않는 메트릭 메서드 삭제. → W2
- Q18 사가 표·결제 대기 상한·멱등 본문: 스펙 표에 COMPENSATING↔STUCK 반영, 결제 대기 상한은 사용자 행 잠금(SELECT … FOR UPDATE)으로, 같은 키에 다른 본문은 422(본문 해시 대조). 없는 주문서 404 유지. → W1
- Q19 운영 ENUM 11개: 해당 도메인마다 VARCHAR 전환 마이그레이션 + 운영 스키마 ↔ 엔티티 대조 점검 스크립트(scripts/ops). → W4
- Q20 재고·창고·이행 REST 소유 검사: 재고·이행은 해당 상품 판매자 또는 어드민, 창고는 어드민 전용. → W1
- Q21 배송비 정산 시점: 「판매자의 첫 확정 라인」으로 바꿔 line:null 경계 이벤트를 없앤다. → W2
- Q22 정산: 스펙 문구 정정(outbox 없음), 역분개 어드민 API, 플랫폼 판매자 1 은 지급 제외(자기 매출), 입금 뒤 환불의 PG 미수금 음수는 「PG 에 돌려받을 돈」으로 수용·문서화, 정산서에 실제 포함 기간(최소~최대 확정일) 표기. → W2
- Q23 DLT: 옛 `-dlt` 토픽은 운영 확인 후 레코드가 있으면 적재, 안 쓰는 JSON KafkaTemplate 삭제, 비샘플 traceId 전파 테스트, 두 번째 Flyway ERROR 원인 규명. seller ops_issue 는 어드민 조치 실패용으로 유지. → W4
- Q24 금액 축소 단계: search:batch 가 price_won 을 읽게 먼저 바꿔 배포한 뒤, 다음 배포에서 옛 컬럼 삭제·NOT NULL. → W4(두 번에 나눠 배포)
