# 봉화 (signal-fire) — 설계 문서

> 캔버스 2D 쿼터뷰 **수성 디펜스 + 영웅 육성**. 봉수대에 불이 오르면 적이 온다.
> 웨이브 사이에 영웅이 구역을 돌며 건물을 세우고, 세운 것은 **설계도 등급**으로 다음 판에 남는다.
> 한국사 무장 영웅을 골라 고구려·고려·조선 세 시대를 통과한다.
>
> 순수 정적 웹 (HTML + JS + Canvas 2D). 빌드 스텝 없음, 런타임 외부 요청 0.

---

## 1. 정체

| 항목 | 값 |
|---|---|
| slug | `signal-fire` |
| 한글 타이틀 | 봉화 |
| 한 줄 | "불이 오르면 온다. 그 사이에 성을 세운다." |
| 장르 | 쿼터뷰 수성 디펜스 + 영웅 육성 (판 단위 반복 + 영구 성장) |
| 런타임 | 캔버스 2D, 정적 파일, 외부 요청 0 |
| 타겟 방향 | 세로 (1080×1440 계열). 모바일 우선 |
| 산출물 | `portal-fe/public/games/signal-fire/` |

---

## 2. 왜 이 게임인가 — 카탈로그의 빈자리

플랫폼 게임 79작을 훑어 확인한 것이다.

**판 단위 디펜스는 있다. 판 사이에 남는 것이 없다.**

| 기존작 | 판 단위인가 | 판 사이에 남는가 |
|---|---|---|
| `gear-bastion` (골렘 25웨이브) | O | X |
| `gate-holdout` (망루 20라운드) | O | X |
| `random-tower-defense` (4인 협동) | O | X |
| `random-card-defense` (족보=타워) | O | X |
| `frost-outpost` (생존 RTS 12분) | O | X |
| `rustline-hold` (좀비 기지 건설) | **X — 연속 세계** | O |
| `archer-outbreak` (좀비 궁수 성장) | **X — 연속 오픈월드 29구역** | O |

**「짧은 판 + 판 사이에 영구히 남는 성장」이 79작 중 비어 있다.** 이 게임이 채우는 자리다.

### 인접작과 갈라지는 지점 — 설계에 못 박는다

`archer-outbreak`이 가장 가깝다(영웅 하나 조작·자동 사격·자원 모아 강화·주둔지 소탕). 갈라지는 축은 셋이고, **셋 중 하나라도 무너지면 「궁수 키우기 한국사판」이 된다.**

1. **세계의 모양** — 그쪽은 600×600 연속 오픈월드를 계속 연다. 이쪽은 **스테이지마다 지형이 새로 생성**되고 판이 끊긴다.
2. **영웅** — 그쪽은 궁수 하나. 이쪽은 **로스터**이고 업데이트마다 추가된다.
3. **테마** — 그쪽은 좀비 도시. 이쪽은 한국사.

좀비 테마를 쓰지 않는 이유도 여기 있다. 플랫폼에 좀비 게임이 이미 넷이다 (`archer-outbreak`, `curfew-siren`, `rustline-hold`, `zombie-march`).

---

## 3. 핵심 결정 — 이 게임을 이 게임이게 만드는 것

### 3-1. 설계도 승계 — 건물은 남지만 건물이 남지는 않는다

지형이 매 판 다르므로 세운 건물을 그대로 옮길 수 없다. 대신 **설계도 등급**이 남는다.

- 궁방을 3등급까지 올려 봤으면, 다음 판부터 궁방을 **처음부터 3등급으로** 지을 수 있다.
- 비용은 매 판 다시 낸다. 판 안 자원(목재·철·군량)은 판마다 0에서 시작한다.
- 설계도 등급을 올리는 재화는 **판 클리어 보상**과 **소탕 스테이지 보상**으로만 나온다.

이 한 줄이 "매번 지으러 다니는 맛"과 "영구히 자란다"를 동시에 성립시킨다. 둘 중 하나를 포기하면 기존 타워 디펜스 다섯 작품 중 하나로 수렴한다.

### 3-2. 웨이브 사이가 본체다

전투는 자동에 가깝다. 건물이 알아서 쏘고 영웅도 사거리 안을 자동 사격한다. **플레이어가 실제로 결정하는 것은 웨이브가 오지 않는 동안 어디로 걸어가느냐다.**

- 자원 더미를 캘 것인가, 적 소부대를 밀어낼 것인가, 빈 구역에 건물을 세울 것인가
- 준비 시간은 유한하고, 세 가지를 다 할 수는 없다

그래서 조작은 **수동 이동 + 자동 사격 + 스킬 버튼**이다. 수동 조준을 넣지 않는 이유는 모바일에서 이동·건설·조준이 한 손가락을 두고 다투기 때문이다.

### 3-3. 챕터 = 시대

시대가 지형·적·건물 양식을 통째로 바꾼다. "스테이지 맵이 다양하다"를 무작위 생성이 아니라 **시대 교체**로 만든다.

| 챕터 | 시대 | 상대 | 지형 성격 |
|---|---|---|---|
| 1 | 고구려 | 선비·후연 | 산성. 절벽과 좁은 길목, 고저차 |
| 2 | 고려 | 거란·몽골 | 개활지. 목책과 기병 돌파, 넓은 정면 |
| 3 | 조선 | 왜군 | 읍성. 수로와 화포, 다중 진입로 |

영웅은 시대와 무관하게 고를 수 있다. 해당 챕터 영웅에게만 보너스가 붙는다 — 시대를 잠금장치로 쓰면 "이순신으로 하고 싶은데 못 한다"가 되고, 그건 로스터의 가치를 깎는다.

---

## 4. 한 판의 흐름

```
스테이지 시작
  → 지형 생성 (월드 200×200, 구역 12 + 읍성 1, 적 진입로 2~4)
  → [준비 시간] 영웅이 걸어다닌다: 자원 채집 · 소부대 격퇴 · 건물 건설
  → 봉수대에 불 → [웨이브] 건물 자동 교전, 영웅은 뚫리는 쪽으로
  → 준비 / 웨이브 반복 (챕터별 8~14회)
  → 마지막 웨이브 격퇴 = 승리 / 성문 체력 0 = 패배
  → 보상: 설계도 재화 · 영웅 경험치
```

**소탕 스테이지 (3의 배수)** — 방어가 아니라 공격이다.

- 성문이 없다. 적 주둔지 2~4곳을 영웅이 직접 뚫는다.
- 주둔지에 다가가면 무리가 **한꺼번에** 일어난다. 준비 없이 들어가면 죽는다.
- 보상: 영웅 승급 재료, 새 설계도 해금, 챕터 보스 (6·12번째)

---

## 5. 영웅

1차 로스터 4명. **업데이트마다 추가하는 것을 전제로 데이터 구조를 짠다** — 영웅은 파일 하나로 정의되고 코드 분기를 만들지 않는다.

| 영웅 | 자동 공격 | 성격 |
|---|---|---|
| 이순신 | 원거리, 관통 | 일직선을 쓸어낸다. 수로가 있는 조선 챕터에서 강하다 |
| 권율 | 중거리 | 근처 방어 건물의 체력·사거리를 올린다 |
| 광개토대왕 | 근접 돌격 | 이동 속도가 빠르고 무리를 밀어낸다 |
| 왕건 | 중거리, 약함 | 자원 채집·건설 속도를 올린다. 전투는 건물에 맡긴다 |

- 스킬 버튼 1~2개 (쿨다운)
- **승급 4단계** — 갑주 색과 장식이 단계마다 달라진다. 재료는 소탕 스테이지에서만 나온다
- 레벨과 승급은 영구히 남는다

### 실존 인물 표현 규칙

- **정부 지정 표준영정을 보고 그리지 않는다.** 그 그림 자체에 화가의 저작권이 있다. 전부 새로 디자인한다.
- 희화화·비하하지 않는다 (사자명예훼손 소지).
- 사망 후 수백 년이 지난 인물이라 퍼블리시티권은 문제되지 않는다.

---

## 6. 건물과 자원

**자원 3종** — 목재 · 철 · 군량. 판 안에서만 유효하고 판마다 0에서 시작한다.

**건물 14종**, 각 5등급 (설계도 등급이 상한을 정한다).

| 분류 | 건물 |
|---|---|
| 공격 | 궁방 · 포루 · 노대 · 총통대 |
| 방어 | 목책 · 석축 · 마름쇠 · 성문 보강 |
| 생산 | 벌목장 · 야철장 · 군창 |
| 지원 | 봉수대 · 의막 · 병영 |

- **봉수대**는 시야와 명중을 올린다. 이름값을 하는 건물이 하나는 있어야 한다.
- **병영**은 아군 보병을 낳는다. 영웅 혼자 모든 방향을 막을 수 없다는 것이 이 게임의 압박이다.

---

## 7. 적

**24종** (챕터당 8종). 아트 마지노선에 따라 **종류마다 실루엣이 달라야 한다** — 색만 바꾼 같은 형태는 같은 적으로 읽힌다.

축은 넷이고 챕터마다 다른 조합으로 나온다: 돌격(빠름·약함) · 중장(느림·단단함) · 원거리(건물을 먼저 깬다) · 공성(성문 전담).

**보스 6기** — 챕터당 2 (중간·최종). 소탕 스테이지에서만 나온다.

---

## 8. 아트 파이프라인

이 플랫폼의 캔버스 게임은 이미지 파일을 쓰지 않는다 (`abyssal-crown` 기준 PNG 0장). 이 게임도 같다. 단 **정하는 단계와 그리는 단계를 나눈다.**

| | 무엇 | 어떻게 |
|---|---|---|
| 초상 일러스트 | 영웅 선택·강화 화면의 큰 그림 | **Claude Design 캔버스에서 먼저 확정**. 실루엣·갑주·팔레트·무기 |
| 인게임 스프라이트 | 실제로 뛰어다니는 개체 | 확정된 초상을 기준으로 **코드로 구워 캐시** |

인게임을 코드로 가는 이유:

- 쿼터뷰는 방향 8 × 상태 4 × 프레임 4라 영웅 하나에 100장이 넘는다. 로스터를 늘릴 수 없게 된다.
- 코드로 구우면 갑주 색·장식만 바꿔 **승급 4단계가 공짜로** 나온다.
- 인게임 크기(약 18 CSS px)에서는 초상 디테일이 보이지 않는다. 그 크기의 격은 명암·그림자·실루엣이 만든다.

### 아트 마지노선 (미달이면 머지하지 않는다)

1. 개체는 구워서 캐시한다. 매 프레임 도형을 쌓지 않는다
2. 명암 3단 (hi/mid/lo). 단색 덩어리 금지
3. 접지 그림자
4. 어두운 외곽선 — 배경이 복잡해도 실루엣이 살아야 한다
5. 개체에 이모지를 쓰지 않는다 (HUD·버튼은 허용)
6. 적은 종류마다 실루엣이 다르다

팔레트는 시대마다 6~10색으로 묶는다.

---

## 9. 수치 계약 — 완성 판정에 들어간다

감으로 정하지 않고 **실측해서 이 표와 대조한다.** 기준 대비 ±8%를 넘으면 반려한다.

### 9-1. 맵 범위

| 항목 | 값 |
|---|---|
| 스테이지 월드 | 200 × 200 유닛 |
| 구역 | 12곳 + 읍성 1 |
| 적 진입로 | 2 ~ 4 |
| 스테이지 총수 | 36 (챕터 3 × 12), 그중 소탕 12판 |

### 9-2. 화면 축척 (전역 가드레일 G1-2)

주인공 높이 = **1.8 월드 유닛** 기준.

| 줌 | 시야 폭 (짧은 변) | 주인공 화면 비율 | 화면/월드 면적 | 용도 |
|---|---|---|---|---|
| 가깝게 | 30 유닛 | **1/16.7** | 4.9% | 교전 |
| **기본** | **40 유닛** | **1/22.2** | **8.7%** | 판정 대상 |
| 전모 | 200 유닛 | — | 100% | 맵 전체 파악 |

- 전략·디펜스 대역 **1/16 ~ 1/24** 안에 두 단계 모두 들어간다
- 기본 줌의 면적비가 **10% 이하**다
- **면적비는 가장 긴 세로 화면(390×844, 2.16:1)으로 잰다.** 3:4 화면으로 재면 5.3%가 나와
  통과하지만 실제 폰에서는 8.7%다. 짧은 화면 기준으로 재고 통과시키면 폰에서 상한을 넘는다 —
  월드를 160에서 200으로 키운 이유가 이것이다 (축척을 깎지 않고 월드를 키운다)
- 최대 축소에서 맵 전모가 한 화면에 들어온다 + **미니맵 상시 표시**
- **주인공 CSS px** — 세로 390×844 기준 기본 줌에서 **17.6 px**, 가깝게에서 23.4 px.
  둘 다 조작 대상 하한(28 px)에 못 미친다. **RTS 축척에서 구조적으로 일어나는 충돌이므로
  축척을 포기하지 않고 표식으로 푼다** — 발밑 고리 + 머리 위 표식 + 자기 광원.

### 9-3. 플레이타임과 진행 깊이

| 항목 | 값 |
|---|---|
| 판 길이 | 챕터 1은 4~6분, 챕터 3은 10~12분 |
| 1회차 클리어 | **8시간 이상** |
| 영웅 | 4명 (1차), 승급 각 4단계 |
| 건물 | 14종 × 5등급 |
| 적 | 24종 |
| 보스 | 6기 |

### 9-4. 감으로 정하지 않는 값

판 길이 · 웨이브 간격 · 건물 비용 · 설계도 재화 획득량은 **조절 페이지를 띄워 눈으로 고른다.** 배포 값으로 추측하지 않는다.

---

## 10. 저장과 시드

- **저장**: `localStorage`. 영웅 레벨·승급·설계도 등급·스테이지 진행·해금 상태
- **시드**: 스테이지 지형은 시드에서 결정적으로 생성한다. `GameDaily` 시드 계약을 따른다
- **시드를 저장에 싣는다.** 안 실으면 이어하기가 다른 지형을 깐다
- **시드를 결과 화면에 보여 준다.** 이상한 판을 신고할 때 그 번호 하나면 같은 판을 다시 깐다
- 난수 소비 순서가 곧 지형이다. 생성 단계를 앞에 끼워 넣지 않는다 — **거르기는 난수를 뽑은 뒤에** 한다

---

## 11. 조작 (모바일 우선)

| 입력 | 세로 모바일 | 데스크톱 |
|---|---|---|
| 이동 | 좌하단 가상 스틱 | 방향키 / WASD |
| 공격 | 자동 (사거리 내) | 자동 |
| 스킬 | 우하단 버튼 1~2 | 스페이스 / Shift |
| 건설 | 구역에 서면 뜨는 버튼 → 건물 선택 | 같음 + 단축키 |
| 줌 | 핀치 + 줌 버튼 | 휠 |

- 조작 대상 버튼은 **28 CSS px 이상**
- 세로·가로 두 방향 모두 지원하고 둘 다 실측한다

---

## 12. 검증 게이트

| 게이트 | 판정 |
|---|---|
| 축척 3수치 | 9-2 표 대비 드리프트 ±8% 이내. CDP 실측값으로 판정 |
| 방향 | 세로 390×844 / 가로 두 방향 실측 스크린샷 |
| 아트 마지노선 | 8장 6항목 전수 |
| 콘솔 | 에러 0 |
| 프레임 | 폰 목표 45, 메뉴·일시정지 20 |
| 벤치마크 일치도 | 배포 갱신마다 **깨끗한 서브에이전트**가 소스를 읽고 판정. 만든 과정을 아는 에이전트를 쓰지 않는다 |
| 회귀 주입 | 게이트를 켰다고 말하기 전에 회귀를 넣어 빨간불을 본다 |

---

## 13. 표현 규칙

- 서빙되는 파일(코드·주석·UI 문구·메타태그)에 타사 게임·상표 명칭은 **0건**이다. 벤치마크 비교는 이 문서에만 둔다
- 영웅 초상은 표준영정을 참조하지 않고 새로 디자인한다
- 실존 인물을 희화화하지 않는다
- 게임 규칙·시스템은 저작권 대상이 아니므로 구조 차용 자체는 제약이 없다

---

## 14. 하지 않는 것

- **멀티플레이·동맹 전쟁** — 상시 서버가 필요하고 OCI 무료 티어 제약과 정면으로 부딪힌다
- **수동 조준** — 모바일에서 이동·건설과 손가락을 다툰다
- **연속 오픈월드** — `archer-outbreak`과 갈라지는 축이다. 판은 반드시 끊긴다
- **과금·확률형 뽑기** — 영웅 해금은 스테이지 진행으로만
- **좀비** — 카탈로그에 이미 넷 있다

---

## 15. 클린룸 인계 슬롯

`docs/standards/game-cleanroom-pipeline.md` 마스터 프롬프트에 그대로 넣는 값이다.

| 슬롯 | 값 |
|---|---|
| 진행 모드 | 수직 슬라이스 우선 — 챕터 1의 스테이지 1~3(일반 2 + 소탕 1)을 목표 품질 100%로 먼저 |
| 장르/컨셉 | 쿼터뷰 수성 디펜스 + 영웅 육성. 판 단위 반복 + 설계도 승계 |
| 벤치마크 | 그래픽·연출은 고해상 자체 아트 파이프라인(절차 생성) 기준 — 개체를 구워 캐시하고 명암 3단·접지 그림자·실루엣 구분을 지킨 수준 이상 |
| 맵 범위 | 월드 200×200, 구역 12 + 읍성 1, 스테이지 36 (챕터 3 × 12) |
| 화면 축척 | 시야 폭 40 유닛 / 주인공 1/22.2 / 세로 390 기준 17.6 CSS px. 줌 3단 30·40·200 |
| 플레이타임 | 1회차 클리어 8시간 이상 |
| 진행 깊이 | 영웅 4 × 승급 4, 건물 14종 × 5등급, 적 24종, 보스 6기 |
| 타겟 방향 | 세로 (1080×1440 계열) 우선, 가로 지원 |
| slug | `signal-fire` |

**슬라이스에 반드시 포함**: 이동·건설·스킬 전부 · 적 3종 · 영웅 1명(승급 1단계 포함) ·
이펙트/사운드/아트 문법 최종본 · 챕터 1 지형 1세트 · 축척 3수치 실측표.

---

## 16. 통합 절차 (메인 세션)

클린룸 완료 보고를 받은 뒤 메인 세션이 한다.

1. `portal-fe/public/games/signal-fire/` 산출물 확인, 콘솔 에러 0
2. `portal-fe/public/games/lib/auth.js` 로 로그인 상태를 읽는다 — **게임이 직접 `localStorage` 를 읽지 않는다**
3. 게임 카탈로그 시드 마이그레이션 (`game/feature/.../gamedb/migration/`) — **적용된 마이그레이션은 되고치지 않는다**
4. 썸네일 · 태그 · 설명문
5. 세로·가로 CDP 실측 스크린샷, 검증 브라우저는 측정이 끝나는 시점에 닫는다
