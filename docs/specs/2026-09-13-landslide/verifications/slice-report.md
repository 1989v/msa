<!-- source: portal-fe/public/games/landslide/ -->
# 「사태(沙汰)」 수직 슬라이스 보고 — 노템전 · 궁수 1v1 vs 봇 · 화산 1

- 일자: 2026-09-14 (클린룸 세션, 2026-09-13 착수)
- 산출물: `portal-fe/public/games/landslide/` (games 서브모듈 커밋 `8e6a031d` → `88fbc38f` → `b6e952c3`, 자기 폴더만). msa 본체는 건드리지 않았다.
- 플레이: `python3 -m http.server 8100` (games 폴더) → `http://127.0.0.1:8100/landslide/`
- 이 보고의 스크린샷은 `verifications/shots/`, 로그는 `verifications/logs/` — 이미지는 파일로만 남겼다.

## 1. slug · 구현 시스템 · 규모

slug `landslide`, 이름 「사태(沙汰)」. 순수 정적(HTML + JS + Canvas), 빌드 없음, 런타임 외부 요청 0, 272KB.
코드 1,983줄(js) + index.html 215줄 + 도구 780줄. 에셋은 클로드 디자인 산출물 셋(`characters.js`·`world.js`·`hud.css`)을 `assets/` 에 그대로 복사(hud.css 는 Google Fonts `@import` 한 줄만 제거).

| 층 | 파일 | 내용 |
|---|---|---|
| 정수 수학 | `js/00-fixed.js` · `00a-sintable.js` | 16.16 고정소수점, 정수 sin 표(0.1° 리터럴), mulberry32, FNV-1a |
| 재질표 | `01-materials.js` | 6종 + 공기 + 얼음: 종류·미끄러짐 문턱(모래1/화산재2/눈3)·열/냉·용암 굳기 |
| 세계 | `02-world.js` | 1600×900 `Uint8Array`, 절차 생성(시드·난수 순서 고정: 지표 노이즈 → 덮개 → 바위 바닥 → 용암 분지 → 분석구 → 현무암 덩어리 → 스폰), 청크 활성/더티, 월드 해시 |
| 정착 | `03-settle.js` | 가루 낙하·가라앉음·미끄러짐, 액체 낙하·압력 퍼짐, 용암+물→현무암, 용암+눈/얼음→물, 용암 3턴 굳기, 착탄 흔들림, 상한 900스텝 |
| 기류 | `04-airflow.js` | 40×20 벡터장 = 기본 바람 + 온도항(용암↑·눈/얼음↓, 500셀 감쇠) + 지형 그림자 |
| 탄도·폭발 | `05-ballistics.js` | 중력 + 항력×(기류−속도), 브레즌햄 셀 충돌, 분화구·흙 부서짐·둔덕·직격/반경 피해. 미리보기·봇·발사가 같은 `simulateShot` |
| 유닛 | `06-units.js` | 결정론 걷기(오르기 4·절벽 12·게이지), 낙하·낙사, 매몰(몸 50%)·용암·익사 판정, 파내기 |
| 턴 | `07-sim.js` | 입력 레코드 `{seat, turn, moveX, angle, power, weapon}` → 코루틴(start·move·shot·impact·settle·resolve), 턴 끝 해시, 재생, 봇 공개 뷰, 지진(30턴 뒤 문턱 −1/턴), 바이옴 바람 프로파일 |
| 봇 | `08-bot.js` | 각도×파워 격자(16×11) → 정제 2단, 기류 포함 실제 탄도, 동점은 비행 짧은 쪽, 못 맞히면 게이지만큼 이동 후보, 세 턴에 한 번 비탈 노림, 오차 폭 3단(각도 ±1.5°/±3°/±5.5°) |
| 렌더 | `10-render.js` | 오프스크린 ImageData(더티 청크만 갱신), 먹선 3셀, 가루 알갱이·액체 물결, `characters.js` SVG 래스터(줌별), 유선·열기둥·입자, 궤적 1/3 점선, 착탄 고리·파편·팝업, 흔들림(≤0.6%)·히트스톱, 배경 패럴랙스 |
| 소리 | `11-audio.js` | WebAudio 신스: 발사·착탄·직격·흘러내림·매몰·낙하·용암·턴 종·지진·승/패·거절·탭·걸음·줌 + 바람 앰비언트 + BGM(뜯는 현 오음계 + 북) |
| 입력 | `12-input.js` | 드래그 조준·탭 이동·휠 줌·키보드, 세로 회전 `rotate(90deg)` + `(x,y)→(y, W−x)` |
| HUD·진행 | `13-ui.js` · `14-main.js` | 디자인 판 06/08/09 배치, 씬(타이틀·플레이·일시정지·결과), 턴 애니메이션, 20초 타이머, `PlatformAdapter.runEnd` |
| 도구 | `tools/` | `gates.sh`(전 게이트 + 회귀 주입) · `lint-sim.sh` · `determinism.mjs`(node/jsc) · `typecheck-bot.sh`(tsc checkJs) · `bots.mjs` · `settle-bench.mjs` · `bury-scenario.mjs` · `gen-sintable.mjs` |

## 2. 조작 · 규칙 표

| 입력 | 동작 |
|---|---|
| 드래그(캔버스 어디서든) | 조준 — 당기는 반대 방향으로 날아간다, 길이 = 파워(50~1000). 궤적 첫 1/3 점선 + 연지 점 |
| 탭 | 이동 목표 x(게이지 120셀, 오르기 4·절벽 12 에서 멈춤). 미리 걷는 것을 보여주고 발사 때 시뮬이 같은 경로를 걷는다 |
| 발사 · Space/Enter | 내 차례에만. 파내기 · X 는 매몰 시에만(그 턴의 행동 전부) |
| ←→ / ↑↓ (Shift 미세) | 각도 1°/0.1° · 파워 20/5 |
| Q / E | 게이지 끝까지 좌/우 걷기 |
| + / − / 휠 | 줌 3단 240 / 480 / 900셀 |
| Esc · M | 일시정지 / 소리 |
| 무배정 키·못 하는 때 | 거절음 + 흔들림 + 토스트 |

규칙: 턴 20초(타임아웃 = 패스, 이동은 유지) · 남의 턴에 미리 조준 · 직격 25 · 반경 30셀 감쇠 15 · 매몰 10/턴 · 낙하 40셀 초과분/2(최대 60) · 용암 30/턴 · 익사 10/턴 · 30턴 뒤 지진(가루 문턱 −1/턴, 3턴) · 마지막 생존이 승리 · 점수 = 승 100 · 격파 30 · 피해 1.

## 3. 게이트 결과

### 3.1 결정론 — 회귀 주입 증거 (`tools/gates.sh`, `logs/gates.txt`)

| 게이트 | 깨끗한 상태 | 회귀 주입 | 결과 |
|---|---|---|---|
| lint-sim (js/00~08 코드에 Math.sin/cos/tan/atan/pow/exp/log/sqrt/random/PI·Date.now·performance.now) | ✓ 0건 | `cosD` 를 `Math.cos` 로 | ✗ `js/00-fixed.js:31` 적발 |
| 같은 입력 로그 두 번 재생 → 같은 해시 (시드 1234·77·2026·9·31337 × 14턴) | ✓ 5/5 동일 | 바람 변동에 `Math.random()` 주입 | ✗ 해시 불일치 (`3f3560` ≠ `1a1e78b7`) |
| 정수 단언 (`S.CHECK_INT`: 탄도 적분 상태·유닛·바람·난수) | ✓ | `GRAVITY = FP*0.30 + 0.5` 주입 | ✗ `non-integer ballistics state` throw |
| **Chrome(V8)/WebKit(JSC) 교차** — node 와 macOS `jsc` 가 같은 시드로 같은 로그·같은 해시 | ✓ 5/5 동일 | (교차 항목은 주입 없음 — IEEE 기본 연산은 엔진 간 동일해 부동소수 주입이 빨간불을 내지 않는다. 그 층은 lint + 정수 단언이 막는다) | — |
| 봇 공개 뷰 (tsc `checkJs`, `tools/types/botview.d.ts` 에 방패 각도 없음) | ✓ | `view.units[0].shieldAngle` 읽기 주입 | ✗ `TS2339 Property 'shieldAngle' does not exist on type 'BotUnit'` |
| 주입이 실제로 적용됐는지 | — | 패턴이 안 맞으면 도구가 종료 코드 2 | 실제로 한 번 헛돌았다(바람 줄을 고친 뒤 옛 패턴이 안 맞아 초록불) → 단언 추가 |

턴 끝 해시는 `st.hashes` 에 쌓이고(FNV-1a: 재질 + aux + 지진 + 유닛 x/y/hp/alive/gauge/buried + 턴 + 바람 + 난수), `S.replay(setup, log)` 가 시드 + 로그로 처음부터 재생한다.

### 3.2 축척 (G1-2, `dev.measure()` CDP 실측 — `logs/flow-*.json`, `shots/`)

| 화면 | 짧은 변이 담는 셀 | 주인공 비율 | 주인공 CSS px | 근거 |
|---|---|---|---|---|
| 데스크톱 1280×720 (기본 줌 480) | **480** | **1/20.0** | **36** | `scale 1.5` = 720/480, 24셀 × 1.5 |
| 폰 가로 844×390 (기본 줌 240, `pointer: coarse` true) | **240** | **1/10.0** | **39** | `scale 1.625` = 390/240 |
| 세로 회전 390×844 (`rotated: true`, 회전 뒤 844×390) | **240** | **1/10.0** | **39** | 같은 값 — 회전이 레이아웃을 그대로 옮긴다 |

최대 축소(900)에서 1600×900 이 1280×720 에 정확히 맞는다(`shots/iter2-full-zoom-after-basins.png`). 예외 선언: 참조작 열람 금지(IP)로 축척 실측을 대신한다 — PRD §3 + 디자인 판 06 값이고, 장르 통례와 어긋날 위험은 사용자 체크에서 판정받는다. 폰은 480 이면 19.5px(G5-2 하한 28 미달)이라 기본 240 + 발밑 팀색 고리 + 머리 위 표식. 누름 대상은 44px 을 지킨다(아래 표).

### 3.3 모바일 두 방향 (`shots/phone-*`, `logs/flow-phone.json`)

| 확인 항목 | 세로 390×844 (회전) | 가로 844×390 |
|---|---|---|
| `pointer: coarse` 인식 | true | true |
| 부팅 → 타이틀 → 플레이 진입 (탭만으로) | 도달 (`Input.dispatchTouchEvent`) | 도달 |
| 핵심 조작 전부 (드래그 조준 · 탭 이동 · 발사 버튼 탭) | 동작 (pending angle 126.9°/712 — 회전 좌표 매핑 검산) | 동작 (36.9°/712) |
| 조작 대상 크기 (주인공) | 39 CSS px | 39 CSS px |
| 터치 타깃 최소 변 | 발사 125×44 · 파내기 125×44 · 줌 44×44 · 칩 56×45 · 결과 행 ≥44 | 같음 |
| 정보 패널이 가리는 면적 | 22.3% | 22.3% |
| 방향 전환 후 레이아웃 자가 복구 | 세로→가로: `rotated false`, 844×390 | 가로→세로: `rotated true`, 회전 뒤 844×390 |
| 랭킹 🏆 버튼 자리(우하단)가 조작을 가리지 않음 | 우하 패널은 발사·파내기 — 통합 시 🏆 가 그 위에 뜨면 겹친다 → **통합 세션 확인 항목** | 같음 |
| 우상단 셸 자리 | `.pan.top-right` 를 `--kgd-chrome-right` 만큼 비켰고 누를 것 없음 | 같음 |
| 콘솔 에러 | 0 | 0 |
| 스크린샷 | `phone-portrait-390x844-rotated.png` · `…-after-rotate-back.png` | `phone-landscape-844x390-aim.png` · `…-bot-turn.png` |

`scripts/lint-game-mobile.py landslide --strict`: 통과 1 · 실패 0 · 경고 0.

### 3.4 성능 · 정착 시간

| 항목 | 값 | 어디서 |
|---|---|---|
| 조준 드래그 중 fps (헤드리스 Chrome, 3초 rAF 카운트) | 데스크톱 60 · 폰 가로 60 | `logs/flow-*.json` (vsync 상한 — 실기 45 는 미측, §6) |
| 정착 최악(합성 화산재 절벽 200×260, 상한 900스텝) | node 47~61ms · 브라우저 벽시계 **1.00초**(시간 기준 페이싱: 900스텝을 1.0초에 나눠 돌린다) | `logs/settle-bench.txt` ①, `logs/flow-*.json` settle |
| 실전 정착 (시드 20 × 봇 30턴, 595턴) | 턴 처리 평균 6.2ms · 최대 37ms · 스텝 분포 [<10,<50,<150,<400,≥400] = 146/259/137/38/15 | `logs/settle-bench.txt` ② |
| 고지 버티기 (정상 −30셀 비탈에 매 턴 한 발) | 최고점이 분석구(안식각)면 **1~3턴**에 24셀↓ · 노이즈 언덕은 8~13턴(분석구 도입 전 실측) | `logs/settle-bench.txt` ③ |
| 봇 조준 시간 | 2~18ms/턴 (176 + 81 + 35 시뮬) | `tools/smoke.mjs` |

### 3.5 봇 300판 (좌석별 승률 · 사망 원인 같은 표, `logs/bots300.txt`)

| 난이도(오차 폭) | 판 | 홍(선공) 승률 | 청(후공) 승률 | 무승부(80턴) | 평균 턴 (최소/최대) | 지진 도달 | 직격률 | 판 밖 | 매몰 사건/판 | 파내기/판 | 사망 원인 | 정착 평균/최대 스텝 | 턴 처리 평균/최대 ms |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 정밀 ±1.5° | 300 | 47.7% | 52.3% | 0 | 22.6 (7/60) | 63 | 26.0% | 0.9% | 0.01 | 0.01 | 직격 295 · 폭발 5 | 53.9 / 900 | 6.1 / 90.9 |
| **보통 ±3° (기본)** | 300 | 48.7% | 46.3% | 15 | 38.3 (10/80) | 183 | 14.8% | 2.3% | 0.01 | 0.01 | 직격 274 · 폭발 10 · 매몰 1 | 68.5 / 900 | 6.5 / 83.3 |
| 거친 ±5.5° | 300 | 34.0% | 42.7% | 70 | 56.3 (10/80) | 272 | 9.5% | 5.0% | 0.03 | 0.02 | 직격 227 · 폭발 2 · 매몰 1 | 72.3 / 900 | 6.5 / 87.6 |

읽는 법: 홍 = 선공. 난이도는 오차 폭뿐(정밀 ±1.5°/±30 · 보통 ±3°/±55 · 거친 ±5.5°/±90). 기본값은 **보통**.
좌석 비대칭을 실측으로 잡았다 — 탐색 순서가 해를 정해(오른쪽은 낮은 각부터, 왼쪽은 높은 각부터) 좌우 명중률이 30.8% vs 20.6% 로 갈렸고, 동점을 비행이 짧은 쪽으로 깨자 29.7% vs 33.2%(600판, 잡음 안)로 맞았다.
사망 원인은 봇 판에서 직격이 거의 전부다 — 봇은 표적 중심을 노리고 스폰 띠(양 끝 400셀)에는 분석구가 없어 산사태·매몰이 봇끼리는 안 일어난다. 매몰·파내기 자체는 시나리오로 검증(§3.7).

### 3.6 죽은 입력 감사 (8씬 × 14키 = 112칸, 실제 KeyboardEvent, `logs/dead-input-matrix.md`)

씬: 타이틀 · 도움말 · 플레이(내 차례) · 플레이(상대 차례 = 미리 조준) · 플레이(비행 중) · 플레이(매몰 상태) · 일시정지 · 결과. 키: Space Enter X ←→↑↓ Q E = − Esc M + 무배정 Z.
**죽은 칸 0 / 112.** 판정은 (씬 전환 | 상태 변화 | 효과음 호출 수) 셋 중 하나. 무배정·못 하는 때는 전부 거절음 + 흔들림. 이 감사가 실버그 하나를 잡았다: 결과 → Esc → 로비로 간 뒤 타이틀 배경을 그리는 클로저가 `G.st`(null) 를 읽어 TypeError 3건 — 고친 뒤 콘솔 0.

### 3.7 매몰 · 파내기 · 산사태 (`logs/bury-scenario.txt`, `shots/desktop-landslide-*.png`)

| 배치 | 결과 |
|---|---|
| 안식각(2:1) 화산재 비탈 **발치**에 선 유닛, 위쪽 비탈에 매 턴 한 발 | 묻힘 21→27→33→40→47→**53%(6턴, BURIED, 매몰 피해 10)** → 파내기 286셀 치움 → 0% |
| 1.5:1 비탈에서 30셀 떨어진 평지 | 8발에도 0% — 흘러내린 가루는 발치에 2:1 둔덕으로 멈춘다 |
| 분석구 정상 −25셀 면에 한 발 (브라우저, 시드 1234) | 정착 900스텝(상한), 정상 403→437(34셀↓), 흘러내림 먼지·연지 열기둥이 같이 보인다 |

### 3.8 콘솔 · 종료 · 플랫폼

- 콘솔 에러/경고 0 — 부팅·플레이·모바일 두 방향·죽은 입력 감사·완주 전부. (AudioContext 는 사용자 활성화 뒤에만 만든다 — 합성 이벤트로는 경고가 났었다.)
- 완주: 봇에게 사람 좌석도 두게 해(`dev.autoTurn`) 14턴에 결과 씬 도달, `PlatformAdapter.runEnd({score: 25, detail: '패배 · 14턴 · 지도 #2026', board: 'practice'})` 요청 본문 확인 (`logs/flow-*.json`).
- 타임아웃(20초) → `weapon 255` 패스 레코드가 로그에 실린다. 턴 29→30 에 지진(`slipShift 1`).

## 4. 비주얼 반복 (3회, 회차별 축)

| 회차 | 점검 축 | 진단 | 조치 | 파일 |
|---|---|---|---|---|
| 1 | ③팔레트 ②배경 깊이 | 용암 분지가 비탈 위에 슬래브로 새어 나왔고, 열기둥 유선이 화면 밖까지 긁힌 붉은 선이었다 | 분지 생성 재작성(호수면 아래로 파고 기슭 절벽 ≤22) · 유선을 판의 결(청자 물결 획 + 입자, 열기둥은 용암 위 220셀 안 S 곡선)로 | `iter1-full-zoom-before.png` → `iter2-full-zoom-after-basins.png` |
| 2 | ①캐릭터 즉시 식별 ⑤UI 선명 (폰) | 폰 가로에서 카메라 오프셋이 절대값이라 주인공이 왼쪽 가장자리·줌 패널 뒤로 숨었고, 「궁수」가 두 줄로 꺾였다. 터치 드래그가 패널에 먹혀 조준이 안 됐다 | 카메라 오프셋을 시야 폭 비례로, 패널 텍스트 nowrap | `phone-landscape-844x390-aim.png` |
| 3 | ④전투 순간 박력 · 판 06/09 대비 | 착탄이 작은 파편뿐, 궤적이 2/3 보임(심사 적발), 이동 게이지가 판과 다른 패널, 타이틀 배경 없음, 분석구가 용암 위에 섰다 | 먹선 착탄 고리 + 한지 섬광 + 파편 46, 궤적 1/3 수정, 게이지를 우하로, 타이틀 뒤 오늘의 지도, 웅덩이 위 분석구 제외 | `desktop-1280x720-aim.png` · `desktop-landslide-cone-settling.png` · `desktop-title.png` |

## 5. PRD · 디자인 판 일치도 심사 (깨끗한 서브에이전트, 소스 + PRD + `art/` 만)

심사관 판정: 요소 표 70여 항목 중 없음 1(근경 바위 소품) · 부분 8 · 나머지 있음. 결정론 grep 코드 0건, IP 명칭 0건, 외부 요청 0건. 심사관이 레포 도구를 직접 돌려 확인한 값(재생 해시 동일 · jsc 교차 동일 · 주입 빨간불)을 근거로 썼다.

| 누락(영향 순) | 처리 |
|---|---|
| 1. 매몰이 실전에서 거의 발동 안 함 | **미달 축**으로 남김(§6). 발치 시나리오는 6턴에 매몰 |
| 2. 궤적 1/3 이 아니라 2/3 (정수 개수를 점 수로 나눔) | **고침** (`10-render.js` `ap.n/2/3`) |
| 3. 용암 원천 웅덩이가 안 굳음 | 설계 결정 — §7 미결(PRD 개정) |
| 4. 봇 공개 뷰 tsc 게이트 성립 불가(plain JS) | **tsc `checkJs` 게이트 신설**(`tools/typecheck-bot.sh`, 빌드 아님) — 주입 빨간불 확인 |
| 5. 바이옴에 바람·열원 프로파일 없음 | **고침** (`S.BIOMES`: 사구 바람 강함 · 설원 하강기류 배율) |
| 6. 봇 레코드에 관측 필드(`plan`·`aimSlope`) | **고침** (`S.lastBotPlan` 으로 분리) |
| 7. 이동 게이지가 판 06 과 다른 패널 | **고침** (우하 패널로) |
| 8. 웹폰트 미번들 | 미달 축(§6) |
| 9. 근경 바위 소품 없음 | 미달 축(§6) |
| 과잉: 용암 피해 이중 적용 | **고침** (턴 끝 한 번) |
| 과잉: `document.title` fps 갱신 | **고침** (제거) |
| 과잉: 착탄 흔들림 · 폭발 둔덕 · 분석구 · 봇 비탈 노림 · 타이틀 시드 입력 | 유지, §7 에 PRD 개정 후보로 |

## 6. 벤치마크 대비 자평 · 미달 축과 도달 방법

하한 = PRD §3 수치 + 디자인 판 06·07·09.

| 축 | 판정 | 근거 / 도달 방법 · 비용 |
|---|---|---|
| 규모: 맵 1600×900 · 시드 · 줌 3단 · 전모 | 동급 | §3.2 |
| 축척 3화면 | 동급 | §3.2 |
| 캐릭터·지형·기류·HUD | 동급 | 판 산출물 그대로, 재질 hex 동일, 유선/열기둥, 06/08/09 배치 |
| 정착 ≤1.5초 · 결정론 · 콘솔 0 · 죽은 입력 0 | 통과 | §3 |
| **실기 ≥45fps · 발열** | **미측** | 헤드리스는 60(vsync 상한)이고 소프트웨어 렌더라 실기를 대신 못 한다. 도달: 폰에서 `dev.measure()` + 30분 플레이 — 통합 세션 항목 |
| **매몰·산사태의 실전 빈도** | **미달** | 봇 300판 매몰 0.03/판, 사망 100% 직격. 기전은 있고(발치 6턴) 배치가 문제다. 도달 방법 셋: ① 스폰을 분석구 발치·능선 근처로(생성 1단계 추가, 30분) ② 포수(반경 큰 총통)가 들어오면 자연히 해결 — PRD 도 궁수는 「읽기」, 포수가 「무너뜨리기」 ③ 봇이 분석구 옆으로 걸어가 서게(전략 후보 1개, 1시간). 사용자 판정 요청: ①을 슬라이스에 넣을지 |
| **판 길이** | 경계 | 봇 vs 봇 보통 평균 40턴 안팎(§3.5). 사람 15초 + 봇 4초 ≈ 6~8분으로 PRD 슬라이스 5~8분 안이지만 상한 쪽. 손잡이: 직격 25 유지 시 반경 피해·매몰이 더 자주 나야 짧아진다(위 ①) |
| **폰트** | 미달 | 판의 둥근 고딕/명조 미번들 — OS 폴백. 도달: OFL 원문 검증 후 woff2 서브셋 번들(~200KB, 1시간) |
| 근경 바위 소품(판 06·07 의 `rockChunk`) | 미달 | 셀 지형엔 현무암 덩어리(파괴 가능)로 대신. 판 그대로 두려면 장식 레이어에 지형 표면 추종 배치(30분) |
| 분석구 실루엣 | 부분 | 2:1 삼각 봉우리가 판 07 의 둥근 덩어리보다 날카롭다. 도달: 정상 8셀을 둥글게 깎는 생성 단계(맨 뒤에 추가, 20분) |
| 게임필 | 동급 | 히트스톱·흔들림(≤0.6%)·팝업·착탄 고리·흘러내림 먼지·승리 슬로모·효과음 15종 + BGM |

## 7. 미결 (사용자에게 묻는다)

1. **용암 원천이 굳는가** — PRD 표는 "3턴 뒤 굳어 현무암". 원천까지 굳으면 화산 맵 열원이 3턴 뒤 전부 사라진다. 슬라이스는 「흘러내린 용암·착탄 반경 안 용암만 굳는다」로 뒀다.
2. **착탄 흔들림(반경 56셀 문턱 −1)과 폭발 둔덕** — 재질표 밖 규칙 둘. 이것 없이는 화살로 산사태가 안 났다. PRD §4 에 넣을지.
3. **파내기 턴에 발사도 되는가** — 지금은 파내기가 그 턴의 행동 전부.
4. **매몰 빈도 손잡이 ①(스폰을 분석구 근처로)** 을 슬라이스에서 할지, 포수까지 기다릴지.
5. **기본 난이도** — 보통(±3°) 로 뒀다. 정밀(±1.5°)은 봇 직격률 25% 로 첫 판에 가혹하다.
6. **디자인 캔버스 주소** — `index.html` 의 `<!-- design: … -->` 에 디자인 시스템 프로젝트 id 로 URL 을 적었다. 실제 공유 주소가 다르면 한 줄 교체.
7. **랭킹 🏆 버튼(우하단)과 발사 버튼 겹침** — 통합 세션이 `lib/platform.js` 를 붙일 때 확인.
8. **관전 카메라** — 상대 턴에 탄도를 따라가게 했다(PRD §13 미결정 항목). 내 유닛에 머무는 쪽이 낫다면 한 줄.

## 8. 남긴 것 · 다음 세션에 넘기는 것

- 슬라이스 밖(구조만 있음): 포수·검사·창병·방패병(`S.CLASSES` 자리), 템전, 팀전·8인, 사구·설원(생성·반응은 있음, 화면 미검증), 온라인(레코드·해시·재생 준비).
- 재검증 한 줄: `cd portal-fe/public/games/landslide && bash tools/gates.sh && node tools/bots.mjs 100 normal`.

---

## 라운드 1 (2026-09-14) — 직전 궤도 · 매몰 손잡이 ① · 미결 판정 반영

사용자 판정 반영: 미결 1(용암 원천 안 굳음) 확정 → 구현 그대로. 미결 2·3·5·6 은 현 구현 유지. games 서브모듈 커밋 `1140d04b`(자기 폴더만).

### A. 직전 궤도 점선 (사용자 요청)

| 항목 | 구현 |
|---|---|
| 유닛별 마지막 발사 궤적 유지 | `G.lastShot[seat]` — `shot` 이벤트의 경로를 복사해 두는 **렌더 상태**(시뮬·해시 밖, `14-main.js`). 그 유닛이 다음에 쏘면 교체, 착탄 뒤에 켜진다 |
| 색·농도 | 내 것 연지 `#A2231D` · 상대 청 `#2B4B63`, 불투명도 0.45, 점 간격 22px(현재 조준은 먹선 14px 간격 1/3 구간) — `10-render.js` |
| 착탄 표식 | 같은 색 × (판 밖이면 없음) |
| HUD | 우하 패널에 `직전 52.0° · 900 · 바람 →4` — 그때 바람도 같이(턴마다 바뀌므로) |
| 재생 뒤 복원 | 해시엔 없다. `S.replay` 가 같은 `shot` 이벤트를 다시 내므로 이벤트를 흘려보내면 같은 궤적이 복원된다 |
| 확인 | 데스크톱 1280×720: `shots/r1-desktop-1280x720-last-trajectories-fullzoom.png`(내 궤적 + 상대 궤적 + × 둘 다) · `…-last-trajectories.png`(기본 줌) / 폰 가로 844×390: `shots/r1-phone-844x390-last-trajectories.png`. HUD 값 `52.0° · 900` · `바람 →4` 를 DOM 에서 읽어 확인. 콘솔 0 |

### B. 매몰 손잡이 ① — 스폰을 분석구 발치로

생성 **맨 뒤** 단계 7 로 붙였다(앞 단계 난수 소비 그대로 — 결정론 게이트 5시드 재통과, 시드 1234 시작 해시는 `1a0e181e` → `e89593c8` 로 바뀐다 = 새 단계가 지도에 들어간 것). 좌석 0·1 스폰 뒤(가장자리 쪽)에 안식각 봉우리(높이 50~90)를 세우고 유닛을 **상대를 향한 면의 발치**에 둔다 — 넘어간 화살이 그 면에 떨어지면 흘러내려 묻는다. 서 있을지 걸어 나갈지(게이지 120)는 플레이어 몫.

봇 300판, 같은 표 (`logs/bots300-round1.txt` vs `logs/bots300.txt`):

| | 보통 · 전 | **보통 · 후** | 정밀 · 전 | **정밀 · 후** |
|---|---|---|---|---|
| 홍(선공) / 청(후공) 승률 | 48.7 / 46.3% | **51.7 / 44.3%** | 47.7 / 52.3% | **51.7 / 48.3%** |
| 무승부(80턴) | 15 | 12 | 0 | 0 |
| 평균 턴 (최소/최대) | 38.3 (10/80) | 38.2 (10/80) | 22.6 (7/60) | 23.0 (7/57) |
| 직격률 | 14.8% | 15.1% | 26.0% | 25.6% |
| **매몰 사건/판** | 0.01 | **0.14** | 0.01 | **0.07** |
| 파내기/판 | 0.01 | **0.12** | 0.01 | 0.05 |
| 사망 원인 | 직격 274 · 폭발 10 · 매몰 1 | **직격 269 · 폭발 15 · 매몰 4** | 직격 295 · 폭발 5 | **직격 289 · 폭발 6 · 매몰 5** |
| 정착 평균 / 최대 스텝 | 68.5 / 900 | 105.1 / 900 | 53.9 / 900 | 103.3 / 900 |
| 턴 처리 평균 / 최대 ms | 6.5 / 83 | 7.4 / 102 | 6.1 / 91 | 7.0 / 94 |

읽는 법: 매몰 사건이 보통 14배(0.01→0.14/판), 파내기 12배. 직격은 여전히 1위(93%) — 목표대로 「난다」이지 「주 사인」이 아니다. 좌석 비대칭은 51.7/44.3(보통, 무승부 12 제외)·51.7/48.3(정밀)로 표준오차(±2.9%p) 안. 봇은 여전히 안 걷는다(0.00/판) — 발치에 서서 묻히면 파낸다. 사람은 걸어 나갈 수 있으므로 실전 빈도는 이보다 낮거나(피하면) 높다(상대를 몰면).

### 회귀 없음
gates.sh 9/9 · 죽은 입력 0/112(`logs/dead-input-matrix-round1.md`) · 모바일 두 방향 콘솔 0 · `lint-game-mobile --strict` 경고 0.

### 남은 미결
미결 2(착탄 흔들림·둔덕)·3(파내기 턴 발사)은 다음 라운드. 미결 4 는 이 라운드로 ①을 넣었다 — 사용자 체크 뒤 ②(포수)·③(봇 이동)은 그대로 후보.

---

## 라운드 2 (2026-09-14) — 방향키 걷기 · 고정 점프 · 놓으면 발사

games 서브모듈 커밋 `02861cc1`(자기 폴더만). 수치와 레코드 형식은 `DESIGN.md` §이동 레코드 · 점프 / §조작.

### A. 이동 이벤트 열 + 고정 점프

| 항목 | 구현 |
|---|---|
| 레코드 | `{seat, turn, moves:[{kind:'walk', x} | {kind:'jump', dir}], angle, power, weapon}` — 시간순, 상한 16. 옛 `moveX` 는 걷기 하나로 읽는다. 봇·타임아웃·재생·해시(유닛 `jumps` 포함) 전부 이 열 |
| 걷기 | ←→ 누르는 동안 프레임당 2셀(같은 방향이면 마지막 걷기를 늘리고, 반대면 새 걷기 = 되돌아가기). 탭·Q/E 유지. 규칙 그대로(오르기 4·절벽 12·게이지 120) |
| 점프 | ↑/Z/버튼. 바라보는 쪽 24셀·위 16셀 정수 포물선(17점 표), 바람 무관, 공중 조작 없음. 게이지 40·턴당 2회, 미만/소진이면 거절음+흔들림. 벽·천장이면 멈춤, 내려오며 바닥이면 착지, 그 뒤 `applyGravity` — 절벽 200셀 낙하 = 60(상한) 실측. 착지는 정착을 부르지 않는다 |
| 미리 걷기 | 클라이언트가 같은 `S.applyMoves` 를 dry 로 돌려 경로·게이지·점프 수를 보여 준다(`G.rebuildMoves`). 발사 때 시뮬이 같은 열을 걷는다 — 표시와 결과가 갈릴 자리가 없다 |
| 봇 | 후보에 점프(좌/우)·걷고 점프를 넣었다(직접 사선 점수 > 24 일 때 평가). **300판에서 0회 사용** — 각도·파워 자유도로 제자리에서 거의 항상 사선이 열린다(합성 처마·턱 시나리오에서도 8° 저각으로 빠져나갔다). 봇이 점프를 쓰게 하려면 위치 가치가 필요 → 미결 |
| 결정론 | 5시드 × 14턴 로그에 **점프 턴을 끼워**(2턴째 점프, 5턴째 걷기+점프×2) 재생·jsc 교차 해시 동일 (`logs/gates-round2.txt`) |

### B. 놓으면 발사

| 항목 | 구현 · 실측 |
|---|---|
| 기본 | 내 차례에 드래그를 놓으면 발사. 로그에 `moves`(걷기+점프 2)가 실린 채 `flight` 진입 확인 |
| 취소 원 | 드래그 **시작점** 둘레 반지름 = 짧은 변 × 0.11(최소 40px): 데스크톱 79px · 폰 43px. 안에서 놓으면 이전 조준으로 되돌리고 「취소」 토스트 — 실측 `pending 500/700` 복원, 턴 그대로. 원 안이면 궤적 미리보기를 숨기고 붉은 원 + 「취소」 라벨 (시작점이 유닛이면 곧 유닛 둘레 — 드래그는 어디서든 시작할 수 있어 원은 시작점에 그린다) |
| 남의 턴 | 놓아도 저장만(`pending 337/501`, 모드 유지) + 토스트 |
| 토글 | 일시정지 메뉴 「놓으면 발사: 켬/끔」, `localStorage sata.releaseFire`(끔 → `0` 저장 확인, 놓아도 `aim` 유지). 발사 버튼·Space/Enter 유지 |
| 첫 안내 | 첫 드래그 한 번 「놓으면 날아갑니다 — 짧게 당기면 취소」(`sata.hint1`) |
| 회전 세로 390×844 | 같은 앱 좌표계로 판정 — 취소 43px 안 놓기 → 복원, 긴 드래그 → 발사(`flight`), 남의 턴 저장 전부 동일 |

### 검증

| 항목 | 결과 |
|---|---|
| gates.sh | 9/9 (점프 포함 로그) — `logs/gates-round2.txt` |
| 죽은 입력 | **11씬 × 22입력 = 242칸, 죽은 칸 0** (`logs/dead-input-matrix-round2.md`). 씬 추가: 게이지 <40 · 점프 2회 소진 · 놓으면 발사 끔. 입력 추가: ↑/Z·A/D/W/S·점프 버튼·취소 구간 놓기·긴 드래그 놓기·무배정 J. 감사가 잡은 것: 패널(타이틀·결과·일시정지) 바탕·카드·버튼을 누르고 끌어 나가면 무반응이었다 → 바탕은 거절음+카드 흔들림, 카드·버튼은 누르는 순간 소리 |
| 두 방향 CDP | 점프 버튼 폰 51×44 · 파내기 51×44 · 발사 57×44 · 줌 44×44(데스크톱 77/77/89×44). 가리는 면적 22.3%. 콘솔 0 |
| fps · 정착 · 완주 | 조준 중 60 · 정착 최악 1.00초 · 자동 완주 11턴 → 결과 + `runEnd` |
| 스크린샷 | 점프 중 `shots/r2-desktop-1280x720-jump-midair.png`(발이 지면 위 14셀) · 취소 구간 `shots/r2-desktop-1280x720-drag-cancel-zone.png` · `r2-phone-844x390-drag-cancel-zone.png` · 폰 가로 버튼 배치 `shots/r2-phone-844x390-hud-jump-button.png` |

### 봇 300판 보통 (라운드 1 → 2, `logs/bots300-round2.txt`)

| | 라운드 1 | **라운드 2** |
|---|---|---|
| 홍(선공) / 청(후공) 승률 | 51.7 / 44.3% | 51.7 / 44.3% |
| 무승부 · 평균 턴 | 12 · 38.2 | 12 · 38.2 |
| 직격률 | 15.1% | 15.1% |
| 이동/판 · **점프/판** | 0.00 · — | 0.00 · **0.00** |
| 매몰 사건/판 · 파내기/판 | 0.14 · 0.12 | 0.14 · 0.12 |
| 사망 원인 | 직격 269 · 폭발 15 · 매몰 4 | 직격 269 · 폭발 15 · 매몰 4 |

같은 시드·같은 봇 판단이라 표가 그대로다 — 점프는 봇 판에 영향이 없다(사람만 쓰는 능력, 위 A 봇 항목). 사람 쪽 효과는 사용자 체크에서.

### 미결(추가)
- **봇 위치 가치** — 높이·발치 회피·용암 회피를 점수에 넣어야 봇이 걷고 뛴다. 넣으면 봇이 분석구에 올라가 묻히는 쪽으로도 갈 수 있어 사망 원인 분포가 바뀐다.
- 키 계약 개정(보충 반영): 걷기 ←/→ · 점프 ↑/Z · 각도 A/D(Shift 0.1°) · 파워 W/S(Shift 5). ↓ 는 무배정(거절음+흔들림). 실측: A −1.0° · Shift+A −0.1° · S −20 · Shift+S −5 · ↓ 변화 0(거절). 도움말·키 계약 한 줄·매트릭스(242칸 0) 갱신. 옛 각도 키 ←→ 를 누르면 걷기가 된다(무반응 아님).

---

## 라운드 3 (2026-09-15) — 병과 둘(포수·검사) · 온라인 1v1 락스텝 + 로비 표준

games 서브모듈 커밋 `d3aa62d9`(자기 폴더만). 읽은 문서에 `docs/standards/online-versus-lobby.md` 가 더해졌다(아레나 폴더는 열지 않았다).

### A. 병과 — 궁수 · 포수 · 검사

| 병과 | 무기 | 수치(확정) | 봇 전략 |
|---|---|---|---|
| 궁수 | 각궁 | v24 · 항력 0.0045 · 직격 25 · 분화구 12(바위 5) · 반경 15/30셀 · 흔들림 56 | 각도×파워 격자 + 정제 2단 |
| 포수 | 총통 | v21 · **항력 0.0012**(무거워 기류 영향 작다) · 직격 30 · **분화구 20(바위 9)** · 반경 12/50셀 · **흔들림 96** | 같은 탐색 + 큰 반경이 알아서 값을 한다 |
| 검사 | 검기 / 강타 | 검기 = 직선 200셀 절단선(각도만·파워 무시), 선분 위 12, 중심선 제거 + 양옆 흙→모래 / 강타 = 닿는 적 25 + 밀어내기 60셀(착지 낙하 규칙), **이동 게이지 절반 이상** | 닿으면 강타 → 200셀 안이면 검기 → 아니면 접근(절반 남김) → 막히면 점프 → 그래도 막히면 앞을 벤다 |

- 무기는 병과 소속이다 — 레코드의 `weapon` 이 남의 병과 것이면 **자기 기본기로 읽는다**(어떤 레코드가 와도 전원이 같은 답. 온라인에서 조작된 레코드가 와도 시뮬이 갈리지 않는다).
- 병과 선택: 타이틀(연습)·대기실(온라인) 둘 다. `characters.js` 의 `gunner`·`sword` 도형 그대로. 봇 병과는 지도(시드)가 정한다.
- **디자인 산출물의 XML 결함을 하나 우회했다** — `characters.js` 의 포수·검사 도형에 한 요소에 `stroke` 가 두 번 적힌 곳이 있어 SVG 이미지로 래스터하면 **그림 전체가 안 나온다**(HTML 은 앞의 것을 쓰고 넘어간다). 렌더가 중복 속성만 떼어 낸다(`R.xmlSafe`) — 도형은 그대로다. 아트 원본을 고치려면 클로드 디자인에서 고쳐 받아야 한다 → 미결.
- 포수 밸런스: splash 22 → 12 로 내렸다(궁수 대비 승률 73.3% → 69.0%). 근거는 스윕 실측(`22/16/12/10/6`) — 22 는 반경만으로 이기고, 6 은 큰 분화구의 값이 사라졌다.

#### 봇 매치업 300판 × 6 (보통, `logs/matchups-round3.txt`)

| 매치업 (홍-청) | 홍 승률 | 청 승률 | 무승부(80턴) | 평균 턴 | 매몰/판 | 검기/강타 | 점프/판 | 사망 원인 |
|---|---|---|---|---|---|---|---|---|
| 궁수-포수 | 30.3% | **69.0%** | 2 | 32.9 | 0.67 | — | 0.00 | 직격 249 · 폭발 34 · 매몰 15 |
| 궁수-검사 | **81.0%** | 3.0% | 48 | 47.8 | 0.33 | 4935/41 | 2.44 | 직격 202 · 용암 22 · 폭발 12 · 매몰 8 · 강타 4 · 검기 3 · 낙사 1 |
| 포수-검사 | **91.7%** | 0.7% | 23 | 37.8 | 0.42 | 3830/11 | 2.75 | 직격 184 · 폭발 49 · 용암 31 · 매몰 12 · 검기 1 |
| 궁수-궁수 | 51.7% | 44.3% | 12 | 38.2 | 0.14 | — | 0.00 | 직격 269 · 폭발 15 · 매몰 4 |
| 포수-포수 | 50.7% | 49.3% | 0 | 30.0 | **0.91** | — | 0.00 | 직격 220 · 폭발 56 · **매몰 24** |
| 검사-검사 | 25.0% | 29.3% | 137 | 51.5 | 0.46 | 9167/138 | 2.12 | 검기 113 · 용암 20 · 강타 20 · 매몰 10 |

**판정**: 동병과 셋은 선후공 대칭(50±2%p). 포수는 궁수보다 세다(69%) — 반경·흔들림이 지형을 무너뜨려 매몰이 4.8배(0.14 → 0.67). **검사는 봇 손에서 3% 이하** — 목표 0.8~1.2 밖이다.
**봇 한계**(근접은 봇이 공정히 못 잰다)와 **설계 문제**를 갈라 적는다. 실측: 시작 거리 평균 **1134셀**, 검사 봇의 턴당 전진 **32.5셀**(걷기가 12셀 넘는 내리막에서 멈추고 점프는 24셀), **60판 중 접촉 0회**. 검사의 사거리는 검기 200 뿐이라 접촉 전에 직격으로 죽는다(사망 42/60 직격).
- **봇 한계인 부분**: 사람은 엄폐·지형을 이용해 접근하고, 검기로 발밑을 잘라 낙사·매몰을 노린다 — 봇은 「직선 사거리 안에 적이 있나」만 본다.
- **설계 문제인 부분**: 1v1 스폰 거리 1134 대 이동 300/턴 + 직선 200 은 **네 턴 이상 맞고만 있으라**는 뜻이다. PRD §6.1 의 삼각(근접이 원거리를 문다)은 8인·엄폐가 많은 판을 전제한다. 손잡이는 ① 1v1 스폰 거리 축소(600~700) ② 검사 이동 300 → 400 ③ 검기 사거리 200 → 300 ④ 근접 게이지 규칙 완화. **수치는 사용자 판정 사항** → 미결.

### B. 온라인 1v1 락스텝 + 로비 표준

구현: `js/15-net.js`(릴레이 클라이언트, 상한·유휴 자체 계측) · `js/16-lobby.js`(진입로 다섯·대기실·공유) · `js/14-main.js` 의 락스텝(레코드 방송·해시 비교·재생 복구·봇 승계) · `tools/dev-relay.mjs`(표준 §5 를 흉내 내는 개발 릴레이, `/stats` 계측) · `tools/e2e-online.mjs`(게이트).

#### 온라인 게이트 (표준 §7) — `logs/e2e-online-round3.json`

| 항목 | 결과 |
|---|---|
| 코드 방 만들기 → 대기실 | ✓ 코드 6자(0·O·1·I 없는 알파벳) |
| 코드로 입장 → 같은 대기실 · `hi` 로 좌석 채움 | ✓ (2석 legacy 라 `seat` 알림이 없다) |
| 대기실 병과 선택이 남의 칸에 보인다 · 채팅 | ✓ |
| 준비 안 한 사람 있으면 한 번 막고 알린다 → 한 번 더 누르면 시작 | ✓ |
| 방장 시작 → 둘 다 **같은 릴레이 시드**로 매치 | ✓ |
| **한 판 끝까지(두 사람 입력) · 턴마다 해시 일치** | ✓ 21~36턴 · 불일치 0/0 |
| 순위표 보드 | ✓ `board:'online'` |
| 결과 → 같은 대기실(준비 초기화) · 한 판 더(라운드 2) | ✓ 손님이 먼저 누르면 「방장이 대기실을 여는 것을 기다린다…」 |
| 상한 계측 | ✓ **최대 5 msg/s · 104자** (상한 40/s · 4,096자) · 한 판 좌석당 22~63 메시지 |
| 상대 탭 닫기 → 봇 승계 | ✓ **252~254ms** (기준 2초) |
| 빠른 대전 둘이 만나 시작 | ✓ |
| 공개 방 목록에서 입장 | ✓ |
| `?room=CODE` 입장 · 초대 링크 형식 | ✓ `https://game.1989v.com/games/landslide?room=<CODE>` |
| **디싱크 주입 → 불일치 감지 → 로그 재생 복구 → 이후 해시 일치** | ✓ 불일치 1 · 복구 1 · 이후 두 턴 일치 |
| **영구 주입(중력) → 재생해도 불일치 → 관전 강등 · 방장이 봇으로 잇는다** | ✓ 주입이 실제로 시뮬을 바꿨는지 먼저 확인(착탄 1150 → 1072) |
| 모바일 두 방향 로비·대기실 탭만으로 · 누름 ≥44px | ✓ 가로 최소 50px · 세로 142px |
| 콘솔 0 | ✓ 전 페이지 |

**온라인 게이트 17/17** (E2E 항목 수 기준 27 단계 전부 ✓).

세 가지를 이 과정에서 잡았다.
1. **한 크롬 안의 두 탭은 배경으로 밀려 rAF 가 선다** — 락스텝이 멈춘다. E2E 는 크롬 인스턴스를 둘 쓰고, 게임은 400ms 넘게 프레임이 없으면 타이머로 한 프레임을 돌린다.
2. **합성 터치는 DOM `click` 을 만들지 않는다** — 「탭만으로 된다」를 자동으로 확인할 수 없었다. 게임이 `touchend` 에서 직접 누르고 뒤따르는 click 을 `preventDefault` 로 막는다(실기 동작도 같아진다).
3. **세로 회전에서 `scrollIntoView` 는 축이 돌아 엉뚱한 곳으로 간다** — 패널 `scrollTop` 을 직접 움직이는 `U.reveal()` 로 바꿨다. 폰 대기실은 라벨 줄을 빼고 채팅을 접어 **한 장에 들어온다**(`scrollHeight 390 = clientHeight`, 두 방향).

### 게이트 (gates.sh 11/11)

| # | 게이트 | 결과 |
|---|---|---|
| 1 | lint-sim (+ 회귀 주입) | ✓ / 주입 빨간불 ✓ |
| 2 | 결정론 5시드 — **세 병과 매치업**(시드마다 다른 조합) · 점프 포함 로그 · node/jsc 교차 (+ 주입 둘) | ✓ / 주입 빨간불 ✓✓ |
| 3 | 봇 공개 뷰 tsc (+ 주입) | ✓ / 주입 빨간불 ✓ |
| 4 | `lint-game-mobile --strict` | ✓ 경고 0 |
| 5 | **온라인 E2E**(개발 릴레이 + 헤드리스 둘) | ✓ 27/27 |
| 6 | **병과 매치업 6종** | ✓ |
| 7 | IP 스캔 | ✓ |

죽은 입력: **11 → 13씬 × 24입력 = 336칸, 죽은 칸 0**(`logs/dead-input-matrix-round3.md`). 추가: 병과 선택 화면 · 검사 판 · 포수 판 · 무기 키 1/2.

### 스크린샷
`shots/r3-class-select.png` · `r3-sword-slash-preview.png` · `r3-sword-slash.png` · `r3-sword-melee-throw.png`(밀어내기 포물선 + −25) · `r3-gunner-crater.png` · `r3-online-lobby.png` · `r3-online-room-host.png`(코드·초대 링크·좌석·준비·채팅) · `r3-online-room-ready.png` · `r3-online-public-rooms.png` · `r3-online-match-A.png` / `r3-online-match-B.png`(같은 판을 두 사람이) · `r3-phone-844x390-room.png` · `r3-phone-390x844-room.png`.

### 미달 축 · 미결 (라운드 3)
1. **검사 승률 0.7~3%** — 위 A 의 손잡이 넷 중 무엇을 쓸지 사용자 판정. 아무것도 안 하면 검사는 8인 판에서만 성립한다.
2. **포수 69%** — 궁수 대비. 분화구를 더 줄이면 「무너뜨리기」가 죽는다. 손잡이: 총통 파워 상한(현재 궁수와 같다)·재장전(턴 쉬기)·직격 30 → 27.
3. **`characters.js` 의 중복 `stroke`** — 렌더가 우회하지만 원본을 고치는 것이 맞다(클로드 디자인에서 재발행).
4. **관전** — 표준은 3인 이상 게임만 UI 를 두라고 했고 이 게임은 2석이라 `spectate:true` 로 들어오면 되돌려보낸다. 8인 확장 때 대기실에 체크박스가 필요하다.
5. **재접속(`token`)** — 릴레이가 좌석 토큰을 주지만 아직 쓰지 않는다(탭을 닫으면 봇이 잇는다). 온라인 확장 때.
6. **`?room=` 을 카탈로그 상세가 iframe 으로 넘겨 주는지** — 게임 쪽은 읽는다. 포털 배선은 통합 세션 몫(표준 §4).
7. **`score_boards` 시드** — `PlatformAdapter.init` 에 `practice`·`online` 두 보드를 넣었다. 카탈로그 시드의 `game.score_boards` 도 같은 키여야 한다(메인 세션).
