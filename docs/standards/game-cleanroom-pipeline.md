# 게임 클린룸 파이프라인 — 신작 생산 표준

> **용도**: 게임 신작 1개 = 새 클린 세션 1개. 아래 **마스터 프롬프트**의 `⟨…⟩` 슬롯만 채워
> 새 세션에 그대로 전달한다. 완료 후 **통합 절차**(이 문서 하단)는 메인/통합 세션이 수행한다.
>
> **근거**: 클린룸 실험 2회(abyssal-crown, raging-fist-saga) 실측 — 기존 게임·lib 맥락을 차단한
> 세션이 메인 세션 대비 일관되게 높은 게임 코어 품질을 냈다(기존 관습이 앵커로 작용).
> 품질 마지노선 = **abyssal-crown** (`docs/conventions/game-art-baseline.md` 기준 상향 참조).

## 사용법

1. 새 세션을 연다. **완전 클린룸을 원하면** 레포 밖 빈 디렉토리에서 세션을 시작해 산출물
   폴더만 절대경로로 지정한다 (레포 안에서 시작하면 CLAUDE.md 의 게임 목록이 자동 로드되어
   약한 오염이 생긴다 — 허용 범위지만 알고 쓸 것).
2. 아래 마스터 프롬프트의 슬롯을 채워 첫 메시지로 보낸다.
3. 완료 보고를 받으면 이 문서의 **통합 절차**를 메인 세션에서 실행한다.

## 진행 모드 (2026-08-15 실측 피드백 반영 — 디폴트 변경)

- **디폴트 = 수직 슬라이스 우선.** 전량 생산 후 첫 노출이 아니라, **극초반 1구간**을 목표
  품질 100% 로 먼저 완성해 사용자에게 체크를 받는다. 슬라이스에 반드시 포함:
  핵심 조작 전부(이동/점프/대시/공격/차지 등) · 대표 적 2~3종 · 이펙트/사운드/아트 문법
  최종본 · 대표 배경 1세트 · 벤치마크 나란히 비교표. **슬라이스의 품질이 전체 품질의
  상한이므로, 벤치마크 하한 판정을 슬라이스 단계에서 끝낸다.**
- 사용자 피드백 반영 후 나머지를 완주한다.
- **예외**: 사용자가 "끝까지 완성" / "완주" 를 명시적으로 지시하면 중간 체크 없이 묻지 않고
  끝까지 완성한다.
- 근거: nova-strike 1차 실측 — 전량 생산 후 첫 노출에서 벤치마크(32비트 세대 대표작) 대비
  그래픽·해상도 활용·조작감이 하한 미달(사용자 평가 30점)로 판정. 슬라이스 체크포인트가
  있었다면 초반에 교정됐을 갭이다.

---

## 마스터 프롬프트 (복사용)

```
당신은 브라우저 게임을 처음부터 만드는 시니어 게임 개발자다. 외부 참조 없는 독립
프로젝트로, 아래 요구만을 근거로 게임을 완성하라. 디자인 판단은 질문하지 말고 스스로
결정하라. 막히면 우회 설계로 전진하라.

## 진행 방식
⟨진행 모드 — 디폴트: "수직 슬라이스 우선. 극초반 1구간(핵심 조작 전부 + 대표 적 2~3종 +
이펙트/사운드/아트 문법 최종본)을 목표 품질 100% 로 완성해 벤치마크 비교표와 함께 보고하고
사용자 체크를 받은 뒤, 피드백 반영 후 완주하라." / 명시적 완주 지시 시: "중간 체크 없이
묻지 말고 끝까지 완성하라."⟩

## 과제
⟨장르/컨셉 — 예: "로그라이크 액션", "벨트스크롤+대전격투 커맨드",
"메트로배니아", "시뮬레이션+타워디펜스 하이브리드"⟩
⟨벤치마크 — 예: "그래픽·연출 벤치마크는 32비트 세대 액션 명작 이상"⟩
테마·세계관·이름은 자유롭게 창작하되 slug 는 ⟨slug — 영문 kebab-case, 미정이면 "직접 정하고 보고"⟩ 를 쓴다.

## ★ 품질 게이트 — 이 과제의 핵심. 미달이면 "완성" 선언 금지
이 프로젝트에서 품질은 협상 대상이 아니다. 아래 게이트를 **전부** 넘어야 완성이다.
"동작한다" 와 "완성했다" 를 구분하라.

**⟨벤치마크⟩는 참고가 아니라 하한선이다.** 벤치마크작과 나란히 놓고 아래 축을 비교표로
만들어, 전 축 "동급 이상" 판정이 나와야 완성이다:
①그래픽(스프라이트 밀도·디테일) ②애니메이션(주인공 프레임 수·보간 부드러움)
③해상도·화면 활용 ④이펙트 밀도(피격/폭발/궤적) ⑤조작감(입력 반응·프레임 데이터)
⑥연출(보스 등장·화면 전환·스테이지 도입).
미달 축이 있으면 "미완 명시 후 종료"가 아니라 **개선을 반복**한다. 반복해도 물리적으로
불가능하다고 판단되면 그 시점에 멈추고 사용자에게 갭과 대안을 보고하라 — 미달 상태의
자체 완성 선언은 금지다.

### A. 아트 (가장 자주 미달하는 축 — 최우선 투자)
- **오리지널 디자인 의무 (IP 세이프)**: 벤치마크는 품질·연출의 기준일 뿐이다. 캐릭터·로고·
  고유 디자인을 원작과 유사하게 만들지 않는다 — 마스코트의 실루엣+컬러웨이 조합(예: 파란
  원형 헬멧+얼굴 노출 안드로이드 = 메가맨), 고유 명칭, 트레이드마크 요소 재현 금지.
  "누가 봐도 그 게임 캐릭터" 면 실패. 장르 문법(대시/차지샷 등 메커닉)은 자유
- 해상도: ⟨기본 "1440p급 캔버스 또는 그에 준하는 픽셀 밀도"⟩. 저해상 캔버스를 늘려
  뭉개는 것 금지 (픽셀아트라면 정수 배율)
- **아트 디렉션 일관성**: 팔레트를 정하고 전 요소(캐릭터/배경/VFX/UI)가 같은 문법을 쓴다.
  "에셋 짜깁기 느낌" 이 나면 미달
- 개체 렌더 최저선: 3톤 이상 명암 + 외곽선/림라이트 + 접지(그림자). 단색 도형은 개체가 아니다
- 배경은 **레이어 3장 이상** (하늘/원경/근경 급) + 깊이 연출(파랄랙스·비네트·광원 중 2개 이상)
- 애니메이션: 주인공 기준 대기/이동/공격/피격 최소 4클립, 클립당 프레임이 "끊겨 보이지 않을 것"
- 캐릭터 존재감: 주인공이 화면 높이의 1/5 이상이거나, 그 이하라면 실루엣·발광으로
  어떤 순간에도 즉시 식별될 것
- 에셋: 상용 무료(CC0/OFL 등) 리서치 권장 — **라이선스 원문 검증** 후 CREDITS.md 에
  출처·라이선스·검증 근거 기재. 부족분은 절차 생성(리그 베이크·벡터 등)으로

### B. 게임필 (타격감 불변식)
- 히트스톱 + 화면 흔들림 + 데미지 팝업 + KO/클리어 슬로모 — 전부, 예외 없이
- 플레이어 행동에 응답성 문법: 입력 버퍼, (장르에 맞게) 캔슬/무적 프레임/코요테 타임
- 적 공격에는 반드시 예고(텔레그래프) — 회피가 성립해야 난이도를 올릴 자격이 생긴다
- 60fps 유지 (자체 측정해 보고에 수치 기재)

### C. 콘텐츠 깊이
- 런/스테이지 단위 선택·성장 시스템 ⟨장르에 맞게: 빌드 20종+ / 커맨드 기술표 / 스킬트리 등⟩
- 지역(스테이지) 3개 이상 — 배경·지형·기믹이 **실제로** 달라야 한다 (색만 바꾼 재탕 금지)
- 보스: 지역마다 1기, 패턴 2개 이상 + 페이즈 전환
- 숨김 요소 3종 이상, 메타 진행(죽어도 남는 화폐/해금, localStorage)

### D. 사운드 · UI
- 효과음 + BGM (WebAudio 자체 신스 허용, 외부 오디오 파일 없이 가능). 무음 게임은 미완
- 큼직하고 읽기 좋은 **한국어 UI** — 대형 체력바, 카드/목록형 선택 UI, 폰트는 본문에서
  뭉개지지 않을 것 (한글 웹폰트 자체 번들 권장, 라이선스 검증 포함)

### E. 조작
- PC 키보드 우선. 마우스 조준 금지 — 바라보는 방향 + 조준 보정
- 양손 배치 지원: 방향키+ZXC 와 WASD+JKL 를 모두 받는다

## 기술 제약
- 순수 정적 웹: HTML + JS + Canvas. 빌드 스텝 없음, 런타임 외부 요청 0 (에셋은 폴더에
  포함, favicon 은 data URI)
- 산출물 폴더: /Users/gideok-kwon/IdeaProjects/msa/portal-fe/public/games/⟨slug⟩/
  구성: index.html + js/ + assets/ + CREDITS.md + DESIGN.md
- git: **자기 폴더만** 페이즈별 백업 커밋 허용. 다른 파일은 절대 스테이징하지 않는다.
  (스테이징 전 `git status --porcelain` 로 자기 폴더 외 변경이 섞이지 않았는지 확인)

## IP / 상표 (전 산출물 적용)
- **서빙되는 파일**(코드·주석·UI 문구·메타태그·CREDITS)에는 타사 게임/상표 명칭을 쓰지
  않는다 — 타사 게임명 수식 대신 장르 서술("로그라이크 액션", "대전격투식 커맨드")로
- 벤치마크 비교 서술은 **DESIGN.md 한 곳에만** (통합 단계에서 서빙 경로 밖으로 이동됨)
- 캐릭터·기술·지명은 기존 작품과 겹치지 않게 창작 (메커니즘 자체는 자유)
- 에셋은 라이선스 원문 검증 필수 (기존 조항과 동일)

## 클린룸 하드 룰 (위반 시 실험 무효)
- `portal-fe/public/games/` 아래 **다른 게임 폴더와 `lib/` 를 절대 열람하지 않는다**
- 레포의 게임 관련 문서(`docs/`)·백엔드 모듈(`game/`)도 열람 금지
- 유일한 레포 접근 = 자기 산출물 폴더 쓰기

## 검증 프로토콜 (필수)
- `python3 -m http.server 8100` 으로 서빙, chrome-devtools MCP 로 **자기가 새로 연
  페이지만** 조작한다 (다른 페이지 건드리지 말 것)
- 자동 플레이는 **실제 KeyboardEvent 디스패치**로 — 내부 함수 직접 호출은 검증이 아니다
- 확인 항목: 핵심 조작 전부(커맨드/콤보 포함), 선택·성장 시스템, 스테이지 진행, 보스,
  숨김 요소 1개 이상 실제 발동, 사망/게임오버/재시작, 메타 영속(localStorage 값 검증)
- **콘솔 에러 0** (메시지 0 이 목표 — favicon 404 포함)
- **비주얼 반복 (품질 게이트의 실행 장치)**: 스크린샷 → 진단 → 수정을 **최소 3회**, 각 회차마다
  다음 축을 명시적으로 점검한다 — ①캐릭터가 배경에서 즉시 읽히는가 ②배경에 깊이가 있는가
  ③색이 한 팔레트로 묶이는가 ④전투 순간이 스틸샷으로도 박력 있는가 ⑤UI 글자가 선명한가.
  최종 판정 질문: **"⟨벤치마크⟩와 나란히 놓아도 동급 이상인가"** — 아니오인 축은 하한
  미달이므로 **개선 반복 대상**이다 (숨기는 것도, 보고에 적고 넘어가는 것도 실패다)
- 이미지/스크린샷을 대화(컨텍스트)로 통과시키지 말 것 — 캔버스 캡처가 필요하면
  로컬 업로드 서버(아래 스니펫)를 8101 에 띄우고 페이지에서 POST 해 디스크로 직행시킨다:
  node -e 'require("http").createServer((q,s)=>{s.setHeader("Access-Control-Allow-Origin","*");
  if(q.method!=="POST"){s.end();return}let b=[];q.on("data",c=>b.push(c));q.on("end",()=>{
  require("fs").writeFileSync("/tmp/"+(new URL(q.url,"http://x")).searchParams.get("name"),
  Buffer.from(Buffer.concat(b).toString().split(",").pop(),"base64"));s.end("ok")})}).listen(8101)'

## 실측 함정 목록 (앞선 제작에서 실제로 터진 것 — 같은 함정에 빠지지 말 것)
- 120Hz 디스플레이: 고정스텝이 0회 도는 렌더 프레임에서 edge-trigger 입력을 비우면 입력 유실
- `ctx.filter = 'blur()'` 는 프레임을 죽인다 (33fps 실측) — 그라디언트/스트로크로 대체
- `undefined <= 0` 는 false — 타이머류는 0 으로 명시 초기화하고 부정형(`!(x > 0)`) 비교
- 커맨드 입력은 **입력 당시 facing** 을 히스토리에 함께 기록 (도중 돌아서면 의미 반전)
- 승룡(623)류는 인식창을 좁혀라 — 넓으면 "걷다 파동"이 승룡으로 오폭
- 보스 + 카메라 클램프 조합에서 보스가 화면 밖으로 나가지 않는지 확인 (동적 줌 고려)
- 마지막 적이 스폰되는 프레임에 웨이브 클리어가 나지 않게 스폰 큐까지 판정에 포함
- 오디오는 첫 사용자 입력 후 AudioContext 활성화 (자동재생 정책)

## 최종 보고 양식
1. slug·구현 시스템 목록·규모(모듈/줄수/에셋)
2. (해당 시) 커맨드 표 / 빌드 목록 / 스테이지·숨김 요소 표
3. 자동 플레이 검증 결과 표 + 발견·수정한 실버그 목록
4. 비주얼 반복 횟수와 각 회차 진단→조치
5. 벤치마크 대비 자평 (따라잡은 것 / 못 따라잡은 것) + 남은 미완 명시
6. 플레이 URL + 조작법
```

---

## 통합 절차 (완료 보고 후 — 메인/통합 세션 몫)

1. **스모크 테스트**: 직접 열어 콘솔 에러 0·부팅·기본 조작 확인
   + **IP 스캔**: 서빙 파일에서 타사 게임 명칭 grep, `DESIGN.md` 를 `docs/specs/`
   로 이동(벤치마크 서술은 서빙 경로에 남기지 않는다 — 2026-08-15 전수 점검 정책)
2. **platform.js 배선** (`portal-fe/public/games/lib/platform.js`):
   - index.html 게임 스크립트 앞에:
     ```html
     <script src="../lib/rank.js"></script>
     <script src="../lib/platform.js"></script>
     <script>PlatformAdapter.init({ slug: '<slug>', title: '<한글명>', saveKeys: ['<localStorage 키…>'] });</script>
     ```
   - 게임의 런/스테이지 종료 지점에 한 줄:
     `window.PlatformAdapter && PlatformAdapter.runEnd({ score, detail })`
   - 서버 세이브는 어댑터가 localStorage 가로채기로 자동 동기화
3. **썸네일**: 전투 실플레이 320×180 캡처 → `thumbs/shots/<slug>.png`
   (반드시 업로드 서버 방식 — base64 를 대화로 통과시키지 않는다)
4. **V시드**: `game/feature/src/main/resources/gamedb/migration/V{next}__seed_<slug>.sql`
   - V30 을 템플릿으로. 태그는 V25 생존 슬러그만 매핑(roguelike/leaderboard 등),
     장르는 genre 컬럼. title_en/description_en 필수(SEO). supports_mobile 은 키보드 전용이면 0
   - `./gradlew :game:feature:test --tests '*GameSchemaIntegrationSpec*' --rerun-tasks` 로 실 MySQL 검증
5. **game/CLAUDE.md** 정적 자산 표에 행 추가
6. 커밋 → 푸시 → 배포 확인 → **portal-fe 재빌드**(`gh workflow run images -f services=portal-fe`)
   — 프리렌더/사이트맵은 빌드 시점 API 를 읽으므로 백엔드 반영 후 한 번 더 돌아야 잡힌다

## 슬롯 작성 예 (실전에서 쓴 것)

| 슬롯 | 예시 |
|---|---|
| 장르/컨셉 | "탑다운 로그라이크 액션 RPG를 장르 최고작 완성도로" / "벨트스크롤 액션에 대전격투식 모션 커맨드" |
| 벤치마크 | "현세대 인디 로그라이크 최고작" / "32비트 세대 액션 명작 — 스프라이트·다층 패럴랙스" |
| 콘텐츠 깊이 | "Boon 상응물 최소 20종+희귀도" / "모션 커맨드 3종+·기 게이지 초필·캔슬" |
