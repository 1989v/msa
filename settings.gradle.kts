rootProject.name = "commerce-platform"

include(
    "common",
    "discovery",
    "gateway",
    "product:domain",
    "product:app",
    "order:domain",
    "order:app",
    "search:domain",
    "search:app"
)
