rootProject.name = "commerce-platform"

include(
    "common",
    "discovery",
    "gateway",
    "product:domain",
    "product:app",
    "order:domain",
    "order:app",
    "search:domain",
    "search:app",
    "search:consumer",
    "search:batch",
    "agent-viewer:api",
    "gifticon:domain",
    "gifticon:app",
    "auth:domain",
    "auth:app",
    "code-dictionary:domain",
    "code-dictionary:app"
)
